# دستورالعمل ایجنت

این فایل را ابزارهای کدنویسیِ هوشمند خودکار می‌خوانند. اگر انسان
هستید، [`ROADMAP.md`](ROADMAP.md) §۰ «شروع سریع» را بخوانید — همان
اطلاعات با توضیحِ بیشتر.

---

## پیش از هر ویرایش

```bash
git fetch origin main && git log --oneline HEAD..FETCH_HEAD
```

⚠ اگر چیزی چاپ شد، **اول ادغام کنید**. مخزنِ راه دور دو بار جلوتر از
فضای کار بوده و کارِ انجام‌شده دور ریخته شده. فضای کار بین نشست‌ها
کهنه می‌شود.

## برپا کردن محیط

`node_modules` و مرورگرِ Playwright در snapshot **ذخیره نمی‌شوند**.
هر نشستِ تازه:

```bash
npm ci --no-audit --no-fund
npx playwright-core install chromium
npx playwright-core install-deps chromium
python3 -m http.server 8080 --bind 0.0.0.0 &
```

## گاردها — همه باید سبز باشند پیش از پوش

```bash
npm test               # ۱۴s
npm run test:docs      # ۱s
npm run test:contrast  # ۲s
npm run test:a11y      # ۹s
npm run test:music     # ۱۴s
npm run test:browser   # ۲۰s
npm run test:delight   # ۳۰s
npm run test:streak    # ~۴۰s
npm run test:haptic    # ~۴۰s
npm run test:map       # ~۵s
npm run test:milestone  # ~۲۵s
npm run test:scene      # ~۱۵s
npm run test:games      # ~۱۰s
npm run test:art        # ~۱s
npm run test:gamedepth  # ~۳۰s
npm run test:android   # ~۱s
npm run test:visual    # ۷۴۰s ← فرایندِ پس‌زمینه، نه فرمانِ معمولی
```

⚠ `test:visual` با فرمانِ معمولی timeout می‌خورد. و `tee` بافر می‌کند —
خروجی را از خودِ فرایند بخوانید، نه از فایل.

---

## قوانینِ سختِ مالکِ پروژه

| قانون | جزئیات |
|---|---|
| **نسخه `1.0.0` قفل است** | بدون اجازهٔ صریح بالا نمی‌رود. `test:docs` پاسش می‌دارد. |
| **APK ممنوع** | تا نقطهٔ تکمیل و اجازهٔ صریح. |
| **کلیپ صوتیِ تازه تولید نمی‌شود** | روی ۷۹ ثابت. هرگز TTS. |
| **بی‌صدا باید کار کند** | هرجا صدا نیست، راهنمای دیداری. |
| **پوشِ مکرر** | بعد از هر واحدِ کارِ سبز. |
| **پاسخ به فارسی** | همهٔ ارتباط با مالکِ پروژه فارسی است. |
| **بدون ابزارِ ساخت** | ES Modules استاندارد، بدون bundler. |
| **بدون تکرار** | محتوا یا کدِ تکراری = باگ. |

---

## کارِ بعدی

**قطارِ زیبایی و انیمیشن — `ROADMAP.md` §۷.۱۶.** هشت کارِ مشخص با
ترتیبِ اجرا و شواهد. آیتم‌های ۱ و ۲ (زنجیرهٔ دیداریِ پاسخ‌های درست +
لرزشِ مهربان) کوچک‌اند و اولِ صف‌اند. هیچ‌کدام اجازهٔ APK نمی‌دهند.

---

## سبکِ کد

- **ویرایش با تطبیقِ دقیق.** پیش از هر جایگزینی، لنگر را با `grep`
  بررسی کنید. ⚠ نام‌های فارسی پسوندِ هم‌اند (خانه/رودخانه،
  برگ/بزرگ) — همیشه به آغازِ خط لنگر بزنید: `\n  نام:`.
- **پس از هر ویرایشِ JS، `node --check`.**
- **کامنتِ فارسی، و فقط جایی که «چرا» را توضیح می‌دهد** — نه «چه».
  کامنت‌های موجود پر از `⚠` هستند که تلهٔ کشف‌شده را ثبت می‌کنند؛
  همان الگو را ادامه دهید.
- **کامیتِ فارسی**، با شرحِ باگی که گرفته شد.

## در SVG

- **هرگز `id`** — شکل ممکن است دو بار رندر شود و شناسه تکراری گردد.
- **هرگز `<text>`** — به فونت وابسته است و گلیفِ لاتین می‌آورد.
- **هر تصویر باید روی کارتِ سفید دیده شود** — `test:contrast` می‌سنجد.
- **دو تصویر نباید فقط با برچسبشان فرق کنند.**

## افزودن نوعِ گِردِ تازه — پنج نقطهٔ تماس

اگر یکی جا بماند **خطا نمی‌دهد**، فقط بی‌صدا خراب می‌شود:

1. `case` در `src/core/rounds.js`
2. `SUPPORTED_ROUND_KINDS`
3. `src/core/task-icon.js`
4. `buildStage` / `optionButton` در `src/ui/screens.js`
5. تصویر در `src/core/svg.js`

---

## اصلِ کاریِ این پروژه

> **گاردِ سبز یعنی «آنچه سنجیده‌ام سالم است»، نه «برنامه سالم است».**

هر باگِ جدیِ این پروژه در حالی پیدا شد که همهٔ گاردها سبز بودند:
تصویری با ۰٪ جوهر، پرسشی بی‌معنی در نخستین صفحهٔ نخستین درس، و
صفحه‌ای که افقی می‌لغزید. **برنامه را باز کنید و بازی کنید.**

دو پرسش برای هر گارد:
1. **چه چیزی را نمی‌سنجد؟**
2. **به کجا نمی‌رسد؟** (گاردِ سرریز، سنجهٔ درست داشت ولی هرگز به
   صفحهٔ خراب نمی‌رسید.)

و قانون ۱۰: **هر گاردِ تازه باید منفی-آزمون شود** — باگی که قرار است
بگیرد را عمداً تزریق کنید و ببینید قرمز می‌شود *و پیامِ خوانا می‌دهد*.
