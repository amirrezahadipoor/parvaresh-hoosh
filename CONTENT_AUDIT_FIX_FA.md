# گزارش ممیزی محتوایی و اصلاح رابط پایین صفحه

تاریخ: ۲۰ اوت ۲۰۲۶

## خطاهای محتوایی واقعی که پیدا و اصلاح شدند

1. **نمونه‌های غلط صدای آغازین**
   - «مثلث» به‌عنوان پاسخ کلمه‌ای که با «ث» شروع می‌شود انتخاب می‌شد.
   - «قورباغه» به‌عنوان پاسخ کلمه‌ای که با «غ» شروع می‌شود انتخاب می‌شد.
   - موارد میانی مشابه مانند «لذیذ»، «مژگان»، «رضا»، «نظم» و «ظهر» نیز می‌توانستند وارد بانک صدای اول شوند.
   - بانک `FIRST_SOUND_WORDS` اکنون فقط واژه‌هایی را می‌پذیرد که واقعاً با همان حرف آغاز می‌شوند.

2. **تلفظ مبهم ث و ف**
   - `letter-se.mp3` و `letter-fe.mp3` از نو تولید شدند.
   - ث اکنون صریح می‌گوید: «ثِ سه‌نقطه؛ در فارسی صدای س می‌دهد؛ ثانیه با ث شروع می‌شود.»
   - ف اکنون صریح می‌گوید: «فِ تک‌نقطه؛ صدای ف می‌دهد؛ فیل با ف شروع می‌شود.»

3. **ساعت با پاسخ تصادفی ولی تصویر ثابت**
   - تصویر قبلی همیشه یک زمان ثابت نشان می‌داد اما پاسخ ساعت تصادفی بود.
   - موتور SVG ساعت پویا اضافه شد؛ زاویهٔ عقربه‌ها دقیقاً مطابق پاسخ ساخته می‌شود.
   - semantic audit تطابق ساعت رسم‌شده و گزینهٔ صحیح را کنترل می‌کند.

4. **تصویر، پاسخ را لو می‌داد**
   - سؤال صدای حیوان، تصویر همان حیوان صحیح را نشان می‌داد.
   - سؤال کاربرد عضو بدن، تصویر عضو صحیح را نشان می‌داد.
   - سؤال موقعیت هیجانی، چهرهٔ متناظر پاسخ را در مرکز نشان می‌داد.
   - این stageها با تصویر صوت/علامت سؤال خنثی جایگزین شدند؛ چهره فقط در تمرین واقعی تشخیص چهره باقی می‌ماند.

5. **تصاویر غلط اعضای خانواده**
   - پدر/پدربزرگ/عمو با تصویر «خانه»، برادر با «توپ» و خواهر با «عروسک» نمایش داده می‌شدند.
   - موتور پرترهٔ شخص/خانواده اضافه شد و تمام گزینه‌ها اکنون تصویر انسانی صادقانه و برچسب خودشان را دارند.

6. **صداهای غیرواقعی حیوانات**
   - مواردی مانند «بلبل در آب» برای ماهی، «خرچ‌خرچ هویج» برای خرگوش و «صدای زرنگ و تیز» برای روباه از آزمون صدای حیوان حذف شدند.
   - فقط حیوانات دارای آوای قابل‌شناسایی در سؤال صوتی استفاده می‌شوند.

7. **درس‌های علمی با فعالیت نامرتبط**
   - برای بخش‌های گیاه، چرخهٔ آب، بازیافت، صرفه‌جویی، انرژی و استدلال علمی موتورهای اختصاصی اضافه شد.
   - فعالیت‌های عمومی/نامرتبط مانند داستان روزانه یا طبقه‌بندی تصادفی دیگر جای این مفاهیم را نمی‌گیرند.

8. **درس‌های خواندن با filler نامرتبط**
   - درس ترکیب واژه دیگر بازی حافظهٔ حیوانات اجرا نمی‌کند.
   - درس ردگیری حروف دیگر به نقاشی آزاد تبدیل نمی‌شود.
   - درس ترتیب واژه دیگر مرتب‌سازی اندازه یا داستان عمومی نشان نمی‌دهد.

9. **مسیر عنوان درس‌ها**
   - شرط دامنه برای reading/math/science/social/art/logic اصلاح شد.
   - یک `return null` اشتباه، تمام routeهای عنوان‌محور هنر و منطق را غیرقابل‌دسترسی کرده بود؛ حذف شد.
   - title-driven plan اکنون قبل از packageهای قدیمی اجرا می‌شود.

10. **عنوان‌ها و نوع درس‌های غلط**
    - عنوان‌های نادرست «کلمه‌های دوحرفی: آب، باب، مامان» و «کلمه‌های سه‌حرفی: کتاب، مدرسه» اصلاح شدند.
    - درس‌های زمان، اندازه‌گیری، پول، مقایسه و عدد قبل/بعد در سطح M-L11 type واقعی خود را دارند.
    - اعداد لاتین عنوان‌ها به رقم فارسی تبدیل شدند.

## ممیزی جدید معنا و گزینه‌ها

`npm run audit:semantics` در ۸ پاس برای هر سن ۴، ۵، ۶، ۷ و ۸ سال اجرا می‌شود:

- ۳۴۲ درس
- ۷۰٬۰۰۰ round تولیدی
- کنترل گزینهٔ تکراری/مبهم
- کنترل جواب و محدودهٔ index
- کنترل صدای آغازین کلمات
- کنترل جمع و تفریق
- کنترل بزرگ‌تر/کوچک‌تر
- کنترل ساعت رسم‌شده
- کنترل drag targetها
- کنترل ترتیب مراحل و اندازه‌ها
- کنترل جفت‌های حافظه
- کنترل سایه، ریون، ترازو، بادکنک و tracing
- کنترل ماه‌های فصل، متضادها، صداهای حیوانات و رقم فارسی عنوان‌ها

**نتیجهٔ فعلی: صفر مشکل.**

## صدا

- کلیپ فعال: ۱۰۹۷
- کلیپ جدید/اصلاحی: ۱۶ محتوای علمی + ث و ف بازتولیدشده
- TTS زمان اجرا: صفر
- فرمت همه: MP3 مونو ۲۴ کیلوهرتز
- duplicate باینری: صفر

## مزاحمت دکمهٔ Home/gesture گوشی

در Android 15، edge-to-edge می‌تواند نوار Home/gesture را روی WebView بیندازد و CSS
گاهی inset را صفر گزارش می‌کند. اصلاح انجام‌شده:

- دریافت native `systemBars + displayCutout` در `MainActivity`
- padding واقعی WebView از چهار طرف
- خالی ماندن عمدی ناحیهٔ Home/gesture پایین گوشی
- clearance اضافی CSS برای صفحهٔ اصلی، گزینه‌های درس، arcade و داشبورد والد
- تست Chromium برای حداقل ۱۰px فضای صفحهٔ اصلی و ۲۰px فضای پایین گزینه‌های درس

در نتیجه دکمه‌ها و گزینه‌ها نباید زیر Home/gesture سیستم قرار بگیرند.

## ممیزی دوم: مسیر ورود مستقیم به موضوع و بازسازی کامل ویس‌ها

- resolver ورود مستقیم و ماجراجویی یکسان شد؛ عنوان درس در هر دو مسیر اولویت دارد.
- packageهای قدیمی دیگر نمی‌توانند filler عمومی مثل بادکنک، نقاشی، حیوان یا حافظه را وارد موضوع دیگری کنند.
- جایگزین سنی کاملاً domain-specific شد؛ کودک ۴ ساله در خواندن فقط محتوای خواندن و در علوم فقط محتوای علوم می‌بیند.
- صدای آخر کلمه موتور مستقل گرفت؛ دیگر با سؤال «صدای اول» جایگزین نمی‌شود.
- اتصال تصویر و واژه موتور اختصاصی گرفت؛ حافظه و سایه جای آن را نمی‌گیرند.
- تمام ۷ موضوع از مسیر واقعی UI و کارت سختی در Chromium تست می‌شوند و domain تا داخل Generator کنترل می‌شود.
- بازی غذادادن اصلاح شد: ۶ غذای درست قبلاً اصلاً در گزینه‌ها نبود؛ اکنون هر ۱۲ حیوان همیشه پاسخ قابل انتخاب دارد.

### بازسازی کامل گفتار

تمام فایل‌های قبلی کنار گذاشته و **۱۰۹۷ کلیپ** از transcript مرجع فارسی دوباره تولید شدند:

- voice: `fa-IR-DilaraNeural`
- rate: `-10%`
- pitch: `1.15` با formant shifting (کاهش از 1.28 برای وضوح بیشتر)
- MP3 mono 24 kHz
- هر متن runtime یک clip یکتا؛ اشتراک یک فایل بین چند جمله حذف شد.
- ledger کامل transcript، اندازه و SHA-256 در `narration-provenance.json` ثبت شد.
- ۱۰۱۰ متن runtime و ۱۰۹۷ فایل provenance دارند.
- audit اجازه نمی‌دهد transcript، hash، auto-id، فایل، map یا AVAILABLE_CLIPS از هم جدا شوند.
- تمام ۶۹٬۶۸۰ round نمونه‌برداری‌شده instruction دقیق و clip متناظر دارند.

## ارتقای محتوای نسخهٔ ۳.۳.۰

- چهار داستان تألیفی کوتاه با ۱۲ پرسش درک مطلب اضافه شد: صبح مدرسه، باغچه، دوست تازه و پارک.
- درس‌های داستان، پاراگراف، نتیجه‌گیری و تحلیل شخصیت اکنون از متن و سؤال واقعی استفاده می‌کنند؛ نه واژگان تصادفی.
- شش موتور دستور زبان فارسی اضافه شد: مفرد/جمع، نام زن و مرد، اسم/فعل، هم‌خانواده، فعل مناسب و نشانه‌گذاری.
- عنوان نادرست «مذکر و مؤنث در فارسی» به «نام زن و مرد در واژه‌های آشنا» اصلاح شد؛ فارسی جنس دستوری ندارد.
- جملهٔ نادرست «من دندان مسواک زدم» به «من دندانم را مسواک زدم» اصلاح شد.
- عنوان‌های ادعایی دوکلمه‌ای/سه‌کلمه‌ای که با بانک سؤال تطابق نداشتند، به «جمله‌های کوتاه و ساده» و «جمله‌های کمی بلندتر» اصلاح شدند.
- نمایش متن داستان با کارت خوانای RTL و line-height مناسب اضافه شد.
- دو دستور صوتی دقیق جدید برای درک مطلب و دستور زبان تولید و وارد provenance شدند.
