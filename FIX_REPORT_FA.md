# گزارش اصلاح UI و نسخهٔ اندروید «پرورش هوش کودک»

تاریخ: ۱۴۰۵/۰۵/۲۷ (۲۰۲۶-۰۸-۱۸)

## ایرادهای اصلی پیدا شده

1. آیکون حوزه‌های بازی در صفحهٔ اصلی واقعاً آیکون نبودند و با متن‌هایی مثل «علوم»، «منطق» و «بازی» نمایش داده می‌شدند.
2. آیکون گزینه‌های تصویری Quiz اندازهٔ کنترل‌شده نداشت و در WebView اندروید داخل دکمه‌های دارای `max-height` بریده یا پنهان می‌شد.
3. هدر در گوشی‌های باریک می‌توانست به‌علت مجموع عرض دکمه‌ها، شمارنده و عنوان، کنترل‌های صدا را از صفحه خارج کند.
4. آیکون Launcher اندروید همان آیکون عمومی/قدیمی Android بود و با برند پروژه تطابق نداشت.
5. `webDir` روی ریشهٔ پروژه تنظیم شده بود و فرایند محلی استاندارد و مطمئنی برای ساخت و Sync دارایی‌های وب به Android وجود نداشت. این موضوع می‌توانست باعث اجرای فایل‌های ناقص یا قدیمی در Android Studio شود.
6. Service Worker با cache قدیمی می‌توانست CSS و JavaScript قبلی را نگه دارد و اصلاحات UI را به کاربر نشان ندهد.

## اصلاحات انجام‌شده

- ایجاد کتابخانهٔ آیکون‌های SVG آفلاین در `src/core/icons.js`.
- افزودن آیکون واقعی برای خواندن، ریاضی، منطق، علوم، هوش هیجانی، هنر، شهربازی و مراحل نقشهٔ ماجراجویی.
- اصلاح اندازه و چیدمان تصاویر گزینه‌های Quiz با کلاس `game-option-art`.
- حذف محدودیت ارتفاع مخرب گزینه‌ها و فعال ماندن اسکرول امن صفحهٔ درس.
- افزودن breakpoint برای عرض‌های ۳۸۰ و ۳۳۰ پیکسل و حالت landscape کوتاه.
- بازسازی آیکون‌های Launcher در همهٔ densityها و افزودن adaptive foreground برند پروژه.
- تغییر Capacitor `webDir` به `www`.
- افزودن `npm run build:web`، `npm run android:sync` و `npm run android:debug`.
- اصلاح GitHub Actions برای استفاده از فرایند استاندارد Capacitor Sync.
- افزایش نسخهٔ cache و افزودن فایل آیکون‌ها به precache.
- افزودن تست regression برای رندر شدن حداقل ۶ آیکون SVG در صفحهٔ اصلی.

## نتیجهٔ تست

- ۱۴۲ از ۱۴۲ درس تولید و اعتبارسنجی شدند.
- همهٔ ماژول‌ها، ۶ بازی Arcade و ۵ کاراکتر بدون خطا بارگذاری شدند.
- تست Runtime شامل startup، navigation، quiz interaction و parent gate موفق شد.
- bundle وب مخصوص Android با موفقیت ساخته شد.
- `git diff --check` بدون خطاست.

## ساخت اندروید

نیازمندی پروژه مطابق Capacitor 8:

- Node.js 24
- Java 21

دستورها:

```bash
npm ci
npm test
npm run android:sync
npm run android:debug
```

محیط فعلی Agent دارای Node 20 و Java 11 است؛ بنابراین اجرای خود Gradle/Capacitor در این محیط ممکن نبود. Workflow مخزن Node 24 و Java 21 دارد و پس از Push می‌تواند APK را بسازد.

## نکتهٔ امنیتی بسیار مهم

توکن GitHub داخل پیام گفتگو افشا شده است. آن توکن باید فوراً Revoke شود. برای Push از همان توکن استفاده نشده و نباید دوباره استفاده شود.
