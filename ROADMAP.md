# نقشهٔ راه — پرورش هوش کودک

**این سند مرجع ثابت کار است.** هر وقت مسیر گم شد، به همین‌جا برگرد.

آخرین به‌روزرسانی: ۱۴۰۵/۰۶/۰۴ — افزودنِ §۷.۱۶ (قطارِ زیبایی/انیمیشن)، وضعیت ≈۹۸٪

---

# ۰. شروع سریع — بخوانید اگر تازه به این پروژه رسیده‌اید

این بخش برای کسی است (انسان یا ایجنت) که **هیچ حافظه‌ای از نشست‌های
قبلی ندارد**. بقیهٔ این سند ۱۲۰۰ خط تاریخچه و استدلال است؛ برای
*شروع کردن* فقط همین بخش لازم است.

## ۰.۰ در سه جمله

برنامهٔ آموزشیِ فارسیِ کاملاً آفلاین برای کودکان ۵ تا ۸ سال، با ES
Modules خالص و بدونِ هیچ ابزارِ ساخت. محتوا کامل است (۳۰۷ درس، ۱۸۳۱
تمرین) و هجده گارد از کیفیتش محافظت می‌کنند. **کارِ کارکردی ≈۹۸٪
تمام است؛ آنچه مانده: بسته‌بندیِ اندروید و آزمون روی گوشیِ واقعی
(هر دو نیازمندِ اجازهٔ صریحِ مالک) — به‌علاوهٔ قطارِ زیبایی و
انیمیشن (§۷.۱۶) که آمادهٔ اجراست و اجازه نمی‌خواهد.**

## ۰.۱ نخستین کاری که باید بکنید

⚠ **پیش از هر ویرایش، `git fetch` بزنید.**

این توصیهٔ تشریفاتی نیست؛ دو بار در نشست‌های واقعی اتفاق افتاده که
مخزنِ راه دور **جلوتر** از فضای کار بوده و کارِ انجام‌شده دور ریخته
شده. فضای کارِ ایجنت بین نشست‌ها ممکن است کهنه باشد.

```bash
cd parvaresh-hoosh
git fetch origin main && git log --oneline HEAD..FETCH_HEAD
# اگر چیزی چاپ شد، اول ادغام کنید — بعد کار را شروع کنید
```

## ۰.۲ برپا کردن محیط (هر بار از نو لازم است)

`node_modules` و مرورگرِ Playwright در snapshot ذخیره **نمی‌شوند**.
هر نشستِ تازه:

```bash
npm ci --no-audit --no-fund
npx playwright-core install chromium
npx playwright-core install-deps chromium
python3 -m http.server 8080 --bind 0.0.0.0 &   # سرور باید بالا بماند
```

## ۰.۳ هجده گارد — همه باید سبز باشند پیش از پوش

| گارد | فرمان | زمان |
|---|---|---|
| ساختار + موتور گِرد | `npm test` | ۱۴s |
| صداقتِ مستندات | `npm run test:docs` | ۱s |
| خوانایی تصویر | `npm run test:contrast` | ۲s |
| دسترس‌پذیری | `npm run test:a11y` | ۹s |
| آهنگ | `npm run test:music` | ۱۴s |
| جریان مرورگر | `npm run test:browser` | ۲۰s |
| حس بازی | `npm run test:delight` | ۳۰s |
| زنجیرهٔ دیداری (§۷.۱۶/۱) | `npm run test:streak` | ~۴۰s |
| لرزشِ مهربان (§۷.۱۶/۲) | `npm run test:haptic` | ~۴۰s |
| نقشهٔ سفرِ صحنه‌دار (§۷.۱۶/۳) | `npm run test:map` | ~۵s |
| جشنِ قدم‌های گرد و هوشیِ «وای» (§۷.۱۶/۴ و ۶) | `npm run test:milestone` | ~۲۵s |
| پس‌زمینهٔ صحنه‌ایِ ایستا (§۷.۱۶/۵) | `npm run test:scene` | ~۱۵s |
| گسترشِ بازی‌ها و کارت‌های صحنه‌دار (§۷.۴ و §۷.۱۶/۸) | `npm run test:games` | ~۱۰s |
| ارتقای هنریِ SVG (§۷.۱۶/۷ — نشست‌های پیاپی) | `npm run test:art` | ~۱s |
| عمقِ بازی‌ها (گسترش — شعلهٔ رشته، برجِ پاسخ، مراحلِ حافظه) | `npm run test:gamedepth` | ~۳۰s |
| ظاهر در ۴ اندازه | `npm run test:visual` | **۷۴۰s** |

`npm test` خودش چهارتا را با هم اجرا می‌کند (validate، sync-sw --check،
build-icons --check، test-rounds).

⚠ `test:visual` طولانی است — با ابزارِ فرایندِ پس‌زمینه اجرایش کنید،
نه با فرمانِ معمولی که با timeout می‌میرد. و `tee` بافر می‌کند؛
خروجی را از خودِ فرایند بخوانید.

## ۰.۴ کارِ باقی‌مانده — دقیقاً همین است

**هیچ کارِ کدنویسیِ کارکردیِ باز و مشخصی باقی نمانده.** فهرست §۷.۱۰
(سه کار) در §۷.۱۱ بسته شد. یک قطارِ کارِ باز و مشخص هم هست:
**زیبایی و انیمیشن — هشت کار، §۷.۱۶** که اجازهٔ خاصی نمی‌خواهد.
آنچه مانده:

| # | کار | وضعیت | مانع |
|---|---|---|---|
| ۱ | بازگرداندن `android/` از تگ `v3.3.3-archive` | **سه ناسازگاری — §۷.۱۵** | webDir، versionCode، وابستگی Capacitor |
| ۲ | ساخت APK/AAB و امضا | **ممنوع** | قانون ۳ — نیازمند اجازهٔ صریح |
| ۳ | آزمون روی گوشی واقعی | انجام‌نشده | وابسته به ۲ |
| ۴ | متنِ فروشگاه، تصاویر بازار، حساب توسعه‌دهنده | بیرون از کدنویسی | کارِ مالکِ پروژه |
| ۵ | زیبایی و انیمیشن — هشت کارِ مشخص | **باز — §۷.۱۶** | هیچ؛ آمادهٔ اجرا |

**پس اگر دستور «ادامه بده» گرفتید و اجازهٔ APK ندارید،** کارِ مفید
این است — به ترتیبِ ارزش:

1. **قطارِ زیبایی و انیمیشن — §۷.۱۶.** هشت کارِ مشخص با ترتیبِ اجرا؛
   آیتمِ ۱ (زنجیرهٔ دیداریِ پاسخ‌های درست) یک شکافِ قانونی است، نه
   سلیقه: برنامهٔ بی‌صدا الان رشتهٔ درست‌ها را نشان نمی‌دهد.
2. **برنامه را باز کنید و بازی کنید.** هر باگِ واقعیِ این پروژه
   این‌طور پیدا شده، نه با گارد. §۷.۸ و §۷.۱۱.۱ را ببینید.
3. **عمقِ محتوا در حوزه‌های کوچک‌تر.** «مهارت زندگی» ۲۵ درس دارد در
   برابر ۱۰۲ درسِ خواندن.
4. **گاردِ تازه برای هر باگی که پیدا می‌کنید** — قانون ۱۰.

## ۰.۴.۱ فایل‌های راهنما

| فایل | برای که |
|---|---|
| `AGENTS.md` | ابزارهای کدنویسیِ خودکار — فشرده، فقط دستور |
| `README.md` | چیستیِ پروژه، ساختار، اعدادِ واقعی |
| `ROADMAP.md` §۰ | همین بخش — نقطهٔ ورودِ انسان و ایجنت |
| `ROADMAP.md` §۱–۸ | تاریخچه و *چراییِ* هر تصمیم |
| `docs/AUDIT_2026_08_FA.md` | چرا نسخهٔ ۳ دور ریخته شد |

`npm run test:docs` نگه می‌دارد که اعدادِ این فایل‌ها با دادهٔ واقعی
بخوانند و هیچ‌کدام فرمانِ ناموجود ندهند.

## ۰.۵ پنج تلهٔ این پایگاه کد

اگر فقط پنج چیز بخوانید، این‌ها:

1. **گاردِ سبز یعنی «آنچه سنجیده‌ام سالم است»، نه «برنامه سالم است».**
   ده گارد سبز بودند در حالی که «ماست» با ۰٪ جوهر نامرئی بود و
   نخستین صفحهٔ نخستین درس پرسشِ بی‌معنی داشت.
2. **گارد می‌تواند سنجهٔ درست داشته باشد و باز کور باشد، چون به آن
   حالت *نمی‌رسد*.** سرریزِ بازی حافظه چند مرحله بازیِ بُرده لازم
   داشت تا دیده شود. بپرسید «چه نمی‌سنجد؟» **و** «به کجا نمی‌رسد؟».
3. **نام‌های فارسی پسوندِ هم‌اند** — خانه/رودخانه، برگ/بزرگ، آب/آبی.
   یک regex روی نامِ خالی، «رودخانه» را بی‌صدا به «خانه» تبدیل کرد و
   همهٔ گاردها سبز ماندند. **همیشه به آغازِ خط لنگر بزنید:** `\n  نام:`.
4. **افزودن نوعِ گِرد پنج نقطهٔ تماس دارد** (README §افزودن درس). اگر
   یکی جا بماند، **خطا نمی‌دهد** — فقط واژه به‌جای تصویر نشان می‌دهد.
5. **در SVG هرگز `id` و هرگز `<text>`.** `id` وقتی شکل دو بار رندر
   شود تکراری می‌شود؛ `<text>` به فونت وابسته است و در برنامهٔ فارسی
   گلیفِ لاتین می‌آورد.

## ۰.۶ قوانینِ کاربر که هرگز نقض نمی‌شوند

جدولِ کاملش §۰.۷ پایین است. چهارتای مهم‌تر:

- **نسخه `1.0.0` قفل است** — بدون اجازه بالا نمی‌رود.
- **APK ممنوع** تا نقطهٔ تکمیل.
- **کلیپ صوتیِ تازه تولید نمی‌شود** — روی ۷۹ ثابت. هرجا صدا نیست،
  راهنمای دیداری باشد؛ برنامه باید کاملاً بی‌صدا کار کند.
- **پوشِ مکرر** — بعد از هر واحدِ کارِ سبز، تا هیچ چیز گم نشود.

---

## ۰.۷ قوانین ثابت (هرگز نقض نشوند)

| # | قانون | چرا |
|---|-------|-----|
| ۱ | **نسخه روی `1.0.0` قفل است** | دستور صریح کاربر. تا اجازه ندهد بالا نمی‌رود. |
| ۲ | **درس بدون محتوا وجود ندارد** | فاجعهٔ نسخهٔ قبلی: ۲۰۰ درس از ۳۴۲ توخالی بود. |
| ۳ | **APK نمی‌گیریم تا نقطهٔ تکمیل** | دستور صریح کاربر. |
| ۴ | **هرگز TTS** | WebView اندروید صدای فارسی ندارد. |
| ۵ | **پوش مکرر** | بعد از هر واحد کارِ سبز. |
| ۶ | **بدون تکرار** | هر محتوا/کد تکراری = باگ. مکانیکی بررسی شود. |
| ۷ | **محتوا را از منابع اینترنتی استخراج کن** | نباید نیاز به ورود دستی انسان باشد. |
| ۸ | **بدون شلوغی** | شکست بزرگ نسخهٔ قبلی: سردرگمی کودک و والدین. |
| ۹ | **هیچ گِردی فقط با واژه پرسیده نمی‌شود** | کودک ۵ ساله خواندن بلد نیست؛ واژهٔ تنها یعنی گِرد حل‌نشدنی. (قانون ۱۶) |
| ۱۰ | **هر محافظ تازه منفی-آزمون شود** | محافظِ نیازموده فقط آرامش کاذب است — سه بار محافظِ کور نوشتم. |
| ۱۱ | **عکس صفحه را باز کن و نگاه کن** | هفت باگ واقعی در حالی پیدا شدند که هر پنج آزمون سبز بود. |

## ۰.۸ معیار ارزش

قیمت هدف: **۱۹۰٬۰۰۰ تومان اشتراک همیشگی** در کافه‌بازار.
هدف: ارزش ادراک‌شده **۱۰ برابر** قیمت.

یعنی حداقل: **۳۰۰+ درس واقعی**، **۲۰+ نوع بازی**، **۵ حوزهٔ آموزشی**،
**پنل والدین**، **آفلاین کامل**، **بدون تبلیغ**.

---

## ۱. یافته‌های پژوهشی (مبنای تصمیم‌ها)

### ۱.۱ رابط کاربری کودک ۵ تا ۸ سال

| یافته | منبع | تصمیم ما |
|-------|------|----------|
| هدف لمسی حداقل **۴۸×۴۸dp**؛ برای کودک خردسال **۲cm×۲cm (~۷۵px)** | NN/g, Interaction Design Foundation | `--tap: 76px` |
| فاصلهٔ **۶۴px** بین دکمه‌ها تا لمس اشتباه نشود | IDF | `gap: 16px` حداقل + padding |
| **۳ تا ۵ گزینه** در هر صفحه، نه بیشتر | Univ. of Southern California | حداکثر ۴ گزینه (رده سنی ۸) |
| تمرکز کودک ۴–۶ ساله: **۸ تا ۱۲ دقیقه** | NCES | درس ۵–۷ دقیقه |
| **دکمه‌های پایین صفحه ممنوع** — کودک اشتباهی می‌زند | Smashing Magazine | ناوبری بالا |
| کودک به **هر عمل** انتظار بازخورد دارد | Smashing | صدا + انیمیشن روی هر لمس |
| **انیمیشن زیاد = بار شناختی اضافه** و حواس‌پرتی از هدف | The Learning Standard (نقد Khan Kids) | انیمیشن هدفمند، نه تزئینی |
| متن حداقل **۲۰–۲۴px** | Fruto/Medium | `body 20px`, prompt `26px` |
| **ثبات** مهم‌تر از تنوع: یک رنگ = یک معنی، همیشه | Fruto | توکن‌های رنگی ثابت |
| **انگیزهٔ درونی** بهتر از جایزهٔ بیرونی | Smashing | پیشرفت مهارت، نه اقتصاد سکه |

### ۱.۲ طراحی آموزشی

| یافته | منبع | تصمیم ما |
|-------|------|----------|
| **Scaffolding**: شکستن کار به ریزگام‌ها | Khan Kids / Stanford GSE | هر درس = ۴–۶ گِرد کوچک |
| **بازخورد اصلاحی فوری** — نه فقط «غلط» | Khan Kids | نمایش پاسخ درست + دلیل |
| **راهنمای دیداری/شنیداری** قبل از علامت‌زدن خطا | Khan Kids | `hintDelayMs` سپس هایلایت |
| صدا + تصویر هم‌زمان **بار شناختی را کم می‌کند** برای پیش‌خوان | Mayer, multimedia learning | هر پرسش گفتاری دارد |
| **پیشرفت مبتنی بر تسلط (mastery)** | Khan Kids Learning Path | باز شدن درس بعد با تسلط |
| تکرار فاصله‌دار (spaced repetition) | علوم شناختی | `reviewDays: [1,3,7]` |
| **subitizing** پیش‌نیاز جمع/تفریق است | IES/REL, Clements & Sarama | درس subitizing قبل از جمع |
| توالی: subitizing → شمارش بامعنا → مقایسه → عملیات | IES Number Progression | ترتیب درس‌های ریاضی |
| ترتیب نشانه‌ها ≠ ترتیب الفبا | کتاب فارسی اول | `neshaneh.js` ✅ انجام شد |

### ۱.۳ والدین

| یافته | تصمیم ما |
|-------|----------|
| والدین **بی‌رحم‌ترین داورها** در نظرات فروشگاه‌اند | پنل والدین جدی، نه تزئینی |
| کنترل زمان استفاده لازم است | محدودیت زمان روزانه |
| گزارش پیشرفت واقعی | نمودار مهارت به تفکیک حوزه |
| نگرانی از زمان صفحه | یادآوری استراحت |

---

## ۲. معماری هدف

```
حوزه‌ها (۶)
├── خواندن و نوشتن   ۳۲ نشانه × چند مهارت
├── ریاضی            عدد، شمارش، عملیات، هندسه، اندازه، پول، زمان
├── منطق و الگو      الگو، دسته‌بندی، توالی، حافظه، استدلال
├── تماشا و شناخت    علوم، طبیعت، بدن، محیط
├── مهارت زندگی      عواطف، رفتار اجتماعی، ایمنی
└── زبان انگلیسی     الفبا، واژگان پایه، شنیداری  ← جدید
```

---

## ۳. فازها

- [x] **فاز ۰** — پایهٔ صادق: اعتبارسنج واقعی، ES modules، صداهای سالم
- [x] **فاز ۱** — ترتیب مدرسه‌ای حروف، واژگان مرحله‌ای، ۲۴ شکل SVG
- [x] **فاز ۲ — سیستم طراحی و رابط کاربری** ✔ انجام شد
  - [x] توکن‌های طراحی: رنگ، فاصله، اندازهٔ لمس ۷۶px، تایپوگرافی ۲۰px
  - [x] حذف شلوغی: هر صفحه یک هدف روشن
  - [x] انیمیشن هدفمند (ورود، پاسخ درست/غلط، پیشرفت) — نه تزئینی
  - [x] احترام به `prefers-reduced-motion`
  - [x] `scripts/test-visual.js` — باگ ظاهری را مکانیکی می‌گیرد

### ۲.۱ — قانون «یک تپ تا بازی» (افزوده پس از بازخورد کاربر)

> **مسئله‌ای که کاربر دید:** هر تپ اضافه بین کودک و بازی، لذت را می‌کُشد.
> منوی دوطبقه (اول حوزه، بعد درس) دو مانع بود، و مجموعهٔ درس‌ها تکه‌تکه
> پشت منو پنهان شده بود — همان شلوغی قبلی با ظاهر تازه.

**قانون ۹ (سخت):** کودک باید با **یک** تپ به بازی برسد. هیچ صفحهٔ
واسطی بین «باز کردن برنامه» و «شروع بازی» مجاز نیست. برنامه خودش
می‌داند کودک کجا مانده است؛ پرسیدن از کودک، بار انداختن روی اوست.

**قانون ۱۰ (سخت):** مجموعه باید **یک‌جا** دیده شود. «نقشهٔ سفر» کل
درس‌ها را روی یک خط پیوسته نشان می‌دهد تا حس «یک سفر» بدهد، نه
«فهرست‌های پراکنده». نقشه برای مرور است، نه سر راه بازی.

**قانون ۱۱:** زنجیرهٔ بازی نباید بشکند. پایان هر درس، دکمهٔ درس بعدی
است — نه بازگشت به منو.

**قفل نرم:** نخستین درسِ هر حوزه همیشه باز است (۴ در، نه ۲۳). قفل سفت
کودک را زندانی می‌کند؛ باز گذاشتن همه‌چیز همان فلج انتخاب است. چهار
انتخاب دقیقاً در بازهٔ ۳ تا ۵ گزینهٔ پژوهشی است.

پیاده‌سازی: `src/core/journey.js` (مسیر واحد، حوزه‌ها در هم بافته تا
تنوع حفظ شود بدون شکستن ترتیب داخلی هیچ حوزه‌ای).

### ۲.۲ — قانون «این مقاله نیست، بازی است» (بازخورد کاربر)

> **حرف کاربر:** «اینکه کودک بیاد روی اپلیکیشن مقاله‌ای با تب‌های زیاد
> کار کنه اصلا قابل قبول نیست.»

**قانون ۱۲ (سخت):** هیچ صفحه‌ای نباید شبیه سند یا مقاله باشد.
معیارهای مکانیکیِ «مقاله بودن» که در `scripts/test-delight.js` سنجیده
می‌شوند و شکست‌شان یعنی شکست ساخت:

| معیار | حد مجاز | چرا |
|---|---|---|
| متن پیوستهٔ هر صفحه | **< ۲۰۰ نویسه** | بیش از این یعنی خواندنی، نه بازی |
| پاراگراف‌های پشت‌سرهم | **حداکثر ۱** | دو پاراگراف = مقاله |
| تصویر/شکل در هر گِرد | **دست‌کم ۱** | صفحهٔ فقط‌متن ممنوع |
| تب یا نوار پیمایش | **صفر** | تب برای بزرگسال است |
| عمق پیمایش تا بازی | **۱ تپ** | قانون ۹ |
| نسبت «انجام دادن» به «خواندن» | **بیشتر از ۱** | نقد اپ‌های مشابه |

**قانون ۱۳:** متن طولانی فقط جای یک نفر دارد: پنل والدین. آنجا
بزرگسال می‌خواند، و مقاله بودن اشکالی ندارد — ولی پشت قفل والدین.

**قانون ۱۴:** هر تعامل باید بازخورد دیداری فوری بدهد (< ۱۰۰ms).
کودک باید ببیند که کارش اثر داشت، وگرنه دوباره و دوباره می‌زند.
- [ ] **فاز ۳ — تولید محتوا از منابع اینترنتی** ⬅ *در حال انجام*
  - [x] خواندن: یک نشانه = یک درس (۲۰ درس)، ۵ مهارت
  - [x] ریاضی: توالی IES REL — subitizing، قاب ده‌تایی، تجزیهٔ عدد
  - [x] انگلیسی: Dolch + CVC + Oxford A1 (۶ درس، ۷ نوع گِرد)
  - [x] منطق و علوم: ۸ + ۷ درس، دسته‌بندی بر پایهٔ ویژگی (TRAITS)
  - [x] مهارت زندگی — **ساخته شد** (۸ درس، ترتیب CASEL)
  - [ ] هدف: **۳۰۰+ درس**، همه با محتوای واقعی

  **وضعیت واقعی (اندازه‌گیری‌شده، نه تخمین):**

  | حوزه | درس | گِرد |
  |---|---|---|
  | خواندن و نوشتن | ۲۰ | ۲۰۰ |
  | ریاضی | ۲۳ | ۱۳۸ |
  | منطق و الگو | ۱۴ | ۷۴ |
  | تماشا و شناخت | ۱۳ | ۷۸ |
  | انگلیسی | ۱۲ | ۷۲ |
  | مهارت زندگی | ۸ | ۴۷ |
  | **مجموع** | **۹۰** | **۶۰۹** |

  ۷۳٬۰۸۰ ترکیب گِرد در سه ردهٔ سنی آزموده و سالم.
  ۶۲ شکل SVG (از ۱۸)، ۶۷ نوع گِرد (از ۱۹).

  **درس مهمی که در این فاز گرفتیم:** «۳۰۰ درس» به‌تنهایی معیار بدی
  است. نسخهٔ قبلی پروژه ۳۴۲ عنوان داشت که ۲۰۰ تای آن خالی بود. معیار
  درست: هر درس محتوای واقعی و آزموده داشته باشد، و مولد هر چیزی را که
  نتواند صادقانه بسازد **رد کند** (در ساخت اخیر ۱۱ گِرد رد شد).
- [ ] **فاز ۴ — بازی‌های متنوع** (هدف ۲۰+ نوع)
- [x] **فاز ۴.۵ — ظاهر و انیمیشن جذاب کودکانه** ✔ انجام شد
  - [x] شخصیت راهنما «هوشی» — ۴ حالت معنادار، SVG درون‌خطی
  - [x] جشن پیروزی: هوشی + سه ستارهٔ پیاپی
  - [x] انتقال نرم بین صفحه‌ها (۲۴۰ms) — ولی صفحهٔ بازی نمی‌لغزد
  - [x] تصویر در هر گِرد؛ صفحهٔ فقط‌متن ممنوع
  - [x] `scripts/test-delight.js` — سنجش مکانیکی «کودکانه بودن»
  - [ ] پس‌زمینهٔ زنده — عمداً ساخته نشد (بار شناختی بی‌دلیل)

  **قاعده‌های هوشی (مهم — دوباره باز نکن):**
  ۱. هرگز همزمان با پرسش حرکت نمی‌کند؛ فقط در لحظه‌های گذار.
  ۲. هیچ انیمیشن بی‌پایانی ندارد. آزمون این را مکانیکی می‌سنجد.
  ۳. حالت تزئینی نداریم — هر حالت معنایی دارد.
  دلیل: انیمیشن همزمان با محتوا «بار شناختی بیرونی» می‌سازد و توجه را
  از هدف درس می‌دزدد (نقد وارد بر Khan Academy Kids).
- [x] **فاز ۵ — پنل والدین** ✔ انجام شد
  - [x] گزارش پیشرفت هر حوزه + نمودار هفت روز گذشته
  - [x] محدودیت زمان روزانه (۰ تا ۶۰ دقیقه) با پایان مهربانانه
  - [x] دروازهٔ والدین: ضرب ساده — کودک ۵-۸ ساله رد نمی‌شود
  - [x] پشتیبان‌گیری JSON روی دستگاه
  - [x] آزمون ظاهری وارد پنل می‌شود (منفی-آزمون شد)
### ⭐⭐ فاز ۱۰ — خواندن واقعی، نه شناخت حرف

**سؤال کاربر: «آیا این روال واقعاً فارسی یاد می‌دهد؟» — پاسخ صادقانه
در آن لحظه: نه.**

حوزهٔ خواندن پنج نوع تمرین داشت و هر پنج‌تا در سطح **شناخت حرف**
می‌ماندند: این حرف کدام است، رویش بکش، در کلمه پیدایش کن، حروفش را
بشمار. کودک حرف را می‌شناخت ولی **کلمه را نمی‌خواند**. حلقهٔ گمشده
«صداکشی» بود: بـ ا د ← باد.

**منابع:**
- روش مدرسهٔ ایران (kashkoolaneh.blogfa.com/post/192): اول جدول ترکیب
  صامت+مصوت (رَ، را، رو)، بعد افزودن صامت (رَد، رام، رود)، بعد کلمه:
  «ما» + «دَر» ← مادَر.
- Herlambang 2020: **ترکیب پیش از تجزیه** — ۶۶٪ در برابر ۴۷٪.
- Louisa Moats / IES REL: هجا ← آغازه و قافیه ← واج؛ CV و VC پیش از CVC.

- [x] `src/data/phonics.js` — موتور واج‌شناسی، ۶۲ واژه با نقشهٔ صدا
- [x] سه نوع گِرد تازه: `blend-word`، `syllable-build`، `segment-count`
- [x] مولد خواندن آن‌ها را خودکار می‌سازد → ۱۳۲ به **۲۰۴ گِرد**
- [x] رمزگشایی حالا **۳۵٪** گِردهای خواندن است (پیش‌تر صفر)
- [x] اعتبارسنجی: نقشهٔ صدا باید با املا بخواند
- [x] محافظ نشتی پاسخ برای گِردهای چیدن (منفی-آزمون شد)
- [x] محافظ ترتیب صداکشی در آزمون ظاهری (منفی-آزمون شد)

**اعراب:** فارسی مصوت کوتاه را نمی‌نویسد. «سبد» سه حرف دارد ولی پنج
صدا (س‑اَ‑ب‑اَ‑د). این حدس‌زدنی نیست، پس هر واژه نقشهٔ صدای دستی
دارد. «هـ» پایانی استثناست: صدایش مصوت کوتاه است ولی نوشته می‌شود.

**سه باگ جدی که در همین فاز پیدا شد:**
۱. **`buildLesson` فقط ۶ گِرد اول را برمی‌داشت.** گِردهای صداکشی در
   جایگاه ۸ تا ۱۱ بودند، پس **هرگز به هیچ کودکی نشان داده نمی‌شدند**.
   کل آموزش خواندن واقعی مرده بود. حالا از هر مهارت نمونه برداشته
   می‌شود و رمزگشایی اولویت دارد.
۲. **ترتیب صداکشی وارونه بود.** در چیدمان RTL کودک «اَم» می‌دید
   به‌جای «ما» — تمرین دقیقاً عکس هدفش را می‌آموخت. فقط با
   اندازه‌گیری مختصات واقعی دیده شد، نه با نگاه به کد.
۳. **نشتی پاسخ در بخش‌بندی:** واژهٔ کامل بالای صفحه بود و کودک فقط
   تطبیق شکلی می‌کرد.

**دور دوم — واژگان:** ۶۲ واژه کافی نبود. یازده حرف (ژ ذ ث ح ط ض غ ظ
ص ع ق) هیچ واژهٔ صداکشی نداشتند و هفت درس آخر واژهٔ تازه‌ای نمی‌گرفتند
— کودک در درس «ح» با واژه‌های درس «چ» تمرین می‌کرد.

- [x] بانک واژگان ۶۲ → **۱۰۶ واژه**؛ هر ۳۲ حرف پوشش دارد
- [x] سختی صداکشی با پیشرفت درس بالا می‌رود:
  درس ۳–۶ سه‌صدایی با مصوت بلند → درس ۷–۱۱ + مصوت کوتاه →
  درس ۱۲+ واژهٔ دوبخشی
- [x] واژهٔ یکتای رسیده به کودک: **۲۵ → ۸۴**
- [x] محافظ تنوع واژگان در اعتبارسنج (منفی-آزمون شد)

**باگ چهارم — فروپاشی خاموش بانک واژگان.** سه سطح سختی در سقف ۶
گِردِ درس با هم می‌جنگیدند و فقط ساده‌ترین می‌ماند. بانک ۱۰۶ تایی در
عمل ۲۵ واژه بود و کودک بیست درس پشت سر هم همان‌ها را می‌دید. هیچ
آزمونی این را نمی‌دید چون همه‌چیز «معتبر» بود. **درس: «واژهٔ موجود»
را نشمار، «واژهٔ رسیده به کودک» را بشمار.**

دو اشتباه دادهٔ من که اعتبارسنج گرفت: «حوله» (واو نوشتاری) و «ریاضی»
(یِ اضافه). نوشتار بازسازی‌شده باید با املا بخواند — همین بررسی ساده
دو بار جلوی داده‌ای را گرفت که کودک را گیج می‌کرد.

**دور سوم — سادگی برای سنین پایین.** کاربر این را صریح خواست، پس
مکانیکی سنجیدمش:

- [x] `odd-one-out` تنها گِردی بود که قانون «تعداد گزینه بر حسب سن»
  را دور می‌زد و به کودک ۵ ساله **۴ گزینه** می‌داد. حالا کمینهٔ
  منطقی ۳ است.
- [x] بلندترین پرسش ۴۰ → **۳۳ کاراکتر**؛ «صداها را به هم بچسبان.
  کدام کلمه می‌شود؟» → «کدام کلمه می‌شود؟» (نشان تصویری فعل را
  می‌رساند، جمله نباید تکرارش کند)
- [x] زبان بزرگسالانه حذف شد: «کدام واژه به خانوادهٔ ‎-op تعلق
  دارد؟» → «کدام‌یک ‎-op دارد؟»
- [x] §۲.۹ در `test-delight.js`: ۵ ساله هرگز >۳ گزینه، هیچ پرسشی
  >۳۴ کاراکتر (هر دو منفی-آزمون شدند)

**دور چهارم — چیزهایی که فقط چشم دید.** پس از سبز شدن هر پنج آزمون،
صحنهٔ صداکشی را با چشم نگاه کردم:

- [x] **شکستن خط ترتیب را وارونه می‌کرد.** «شکار» در ۳۲۰px به خط دوم
  می‌شکست و دیداری «ا ک ِ ش ر» می‌شد — واژه‌ای که کودک می‌خواند
  اصلاً واژه نبود. `flex-wrap: nowrap` + کوچک‌شدن کارت‌ها با
  `data-n`. آزمون مختصات هم نگرفته بودش چون فقط یک ردیف را می‌سنجید.
- [x] `min-width: 0` کارت‌های میانی را تا صفر جمع کرد (نامرئی شدند)
- [x] `transform: scale` سرریز افقی می‌ساخت چون فضای چیدمان را کم
  نمی‌کند → به‌جایش متغیر CSS
- [x] قوس پیوند آویزان بود → `align-self: center`
- [x] **باگی که خودم ساختم:** یک ویرایش تکراری
  `r.display.parts.length` را در بلوک `repeat` گذاشت و همهٔ درس‌های
  ریاضی/منطق/علوم با استثنا از کار افتادند — **و هر پنج آزمون سبز
  بودند**، چون هیچ‌کدام به خطای صفحه گوش نمی‌داد.

**محافظ‌های تازه (همه منفی-آزمون شده):**
- `pageerror` و `console.error` در آزمون ظاهری → استثنای زمان اجرا
  دیگر خاموش نمی‌ماند
- درس که باز نشود، آزمون **گزارش** می‌دهد به‌جای اینکه با
  TimeoutError بمیرد
- صداکشی: یک خط، هیچ کارت زیر ۲۴px، بیرون‌نزدن از کادر
- بدترین حالت سنجیده می‌شود (بلندترین واژه از ۴۰ نمونه)، نه یک
  واژهٔ تصادفی

**درس:** پنج آزمون سبز یعنی «آنچه سنجیده‌ام سالم است»، نه «برنامه
سالم است». تا وقتی به خطای زمان اجرا گوش نمی‌دادیم، یک برنامهٔ
شکسته کاملاً سبز بود.

**درس روش‌شناختی:** محافظِ ترتیب صداکشی را سه بار نوشتم و هر سه بار
منفی-آزمون نشان داد **کور** است — خزش هرگز به گِرد صداکشی نمی‌رسید.
نسخهٔ چهارم صحنه را قطعی می‌سازد به‌جای اینکه امید ببندد خزش به آن
برسد. **هر محافظ تازه باید منفی-آزمون شود، وگرنه فقط آرامش کاذب است.**

### ⭐ فاز ۹ — کودکِ پیش‌خوان (مهم‌ترین اصل برنامه)

کودک ۵ ساله خواندن بلد نیست. سنجش نشان داد **۶۷٪ گِردها هیچ صدایی
ندارند** — ریاضی، منطق، علوم و انگلیسی **صفر** کلیپ دارند.

**تصمیم کاربر: صدای تازه تولید نمی‌شود.** ۷۹ کلیپ موجود (حروف الفبا)
سقف نهایی است. به‌جای آن، برنامه باید **کاملاً بدون صدا** قابل انجام
باشد. صدا یک افزودنی است، نه پیش‌نیاز.

**قانون ۱۵ — هیچ گِردی نباید فقط با متن یا صدا قابل انجام باشد.**
  ۱. **نشان تصویری** — کودک از روی شکل می‌فهمد چه کاری باید بکند.
  ۲. **بازخورد شکلی** — تیک سبز / فلش «دوباره»، نه واژهٔ «آفرین».
  ۳. **پیشرَوی خودکار** — هیچ دکمهٔ متنی‌ای لازم نیست.
  ۴. متن و صدا: افزودنی برای والد و کودک بزرگ‌تر.

- [x] `src/core/task-icon.js` — ۱۱ فعل تمرین، نگاشت هر ۳۲ نوع گِرد
- [x] بازخورد شکلی: تیک سبز و فلش نارنجی «دوباره»
- [x] پیشرَوی خودکار در هر چهار نوع تعامل
      (گزینه، چیدن، حافظه، و کشیدن حرف — آخری تازه اضافه شد)
- [x] بلندگو فقط وقتی کلیپ واقعی هست — وعدهٔ دروغ به کودک نمی‌دهیم
- [x] `validate`: هر نوع گِرد باید نشان داشته باشد
- [x] `test-delight`: مسیر کامل بی‌صدا و بی‌خواندن (منفی-آزمون شد)

**درس‌هایی که گرفتم:**
- نشان «بشمار» اول عدد ۳ داشت. در گِردی که پاسخ ۲ بود کودک را
  مستقیماً گمراه می‌کرد. نشان باید بگوید «چه کار کن»، نه پاسخ را.
- نشان «فرق دارد» ضربدر داشت — نشانهٔ «غلط» است و پیش از پاسخ دادن
  بازخورد منفی می‌دهد. بازخورد اشتباه هم ضربدر قرمز نیست، فلش
  «دوباره» است: راهنما باشد نه تنبیه.
- `const finishTrace` در ناحیهٔ مردهٔ زمانی بود و رویداد `pointerup`
  نمی‌توانست صدایش بزند. اعلان تابع (hoisting) حلش کرد.

- [x] **فاز ۶ — تسلط و انگیزه** ✔ انجام شد
  - [x] تکرار فاصله‌دار: ۱، ۳، ۷ روز و سپس هر ۷ روز
  - [x] درس زیر ۸۰٪ زودتر برمی‌گردد (هر روز)
  - [x] مرور جای درس تازه را نمی‌گیرد — هر ۳ درس یک بار
  - [x] گزارش تسلط در پنل والدین + نشان ↻ روی نقشه
  - [x] اعتبارسنجی دادهٔ مرور در `npm test`

  **نکته:** `reviewDays` در ۳۴ درس وجود داشت ولی هیچ‌جا خوانده
  نمی‌شد — دادهٔ مرده بود. حالا زنده است و به ۴۹ درس تسری یافت.
  عمداً هیچ اقتصاد سکه‌ای ساخته نشد: نقد پژوهشی به پاداش بیرونی
  (مدل ABCmouse) وارد است؛ انگیزهٔ درونی پایدارتر است.
- [x] **فاز ۷ — کارایی** ✔ انجام شد
  - [x] بارگذاری **۶۷ms** (هدف <۲۰۰۰ms)
  - [x] ساخت ۸۳۱ گِرد در **۵۶٫۸ms**
  - [x] پاسخ لمس **۰٫۴ms**، حافظهٔ انباشته **۱۰MB**
  - [x] ۴۴ فایل در حافظهٔ نهان → **آفلاین کامل**
  - [x] `scripts/sync-sw.js` پوستهٔ سرویس‌ورکر را از روی دیسک
        می‌سازد؛ CI تفاوت `src/data` و `sw.js` را می‌گیرد.

  **باگ آفلاین:** فهرست دستیِ `sw.js` از فایل‌های واقعی عقب مانده
  بود؛ برنامه در حالت آفلاین نیمه‌بارگذاری می‌شد. فهرست دستی =
  فهرست کهنه. حالا تولیدشونده است.

- [x] **مهارت زندگی — حوزهٔ ششم** ✔ انجام شد
  - [x] ۸ درس به ترتیب CASEL: شناخت حس → آرام‌سازی → همدلی →
        ایمنی شخصی → «نه، برو، بگو» → بهداشت → مهربانی → مرور
  - [x] پنج نوع گِرد تازه: `feel-face`، `name-face`، `safe-pick`،
        `good-habit`، `safety-order`
  - [x] متن ایمنی از راهنمای رسمی بهزیستی؛ در `parentNote` نقل شد
  - [x] ۲۹ شکل SVG تازه (چهره، صحنه، خطر، گام‌های ایمنی)

- [x] **نشان برنامه** ✔ انجام شد
  - [x] `src/core/app-icon.js` پارامتری؛ `build-icons.js` رستر می‌کند
  - [x] هم‌خوانی `theme-color` = `--bg` = پس‌زمینهٔ مانیفست = `#FBF6EF`

- [x] **فاز ۸ — کیفیت نهایی** ✔ انجام شد
  - [x] باگ‌گیری ظاهری در ۴ اندازهٔ صفحه + دو اندازهٔ کوتاه دستی
  - [x] پنج آزمون در CI؛ هر محافظ تازه منفی-آزمون شده
  - [x] بدون ایموجی در پوستهٔ رابط (`ui-icons.js`)
  - [x] هر صفحهٔ حالت‌تغییردهنده پشت دروازهٔ والدین

  **هفت باگی که فقط با نگاه‌کردن به عکس صفحه پیدا شدند** — هر پنج
  آزمون در تمام این مدت سبز بودند:

  ۱. **تنظیمات و «پاک‌کردن پیشرفت» بی‌دروازه بود.** کودک می‌توانست
     ۴۷ ستاره را پاک کند. حالا هر عمل حالت‌تغییردهنده پشت ضرب است.
  ۲. **نمودار هفتگی والدین بریده بود** و روزِ پرتمرین را اشتباه
     نشان می‌داد — یعنی به والد دروغ می‌گفت. علت: درصدِ ارتفاع
     داخل ستون flex نسبت به کل ستون حساب می‌شود، نه فضای آزاد.
  ۳. **بازی حافظه نامِ شکل‌ها را به‌صورت متن نشان می‌داد** («سیب»،
     «ماهی»). بازی تصویری برای کودکِ پیش‌خوان، متنی شده بود.
  ۴. **`pattern-next` کلیدی می‌فرستاد که رابط نمی‌شناخت** (`shape`
     به‌جای `geo`) — گزینه‌ها واژهٔ خالی می‌شدند و **یک گزینه هیچ
     برچسبی نداشت: دکمهٔ نامرئی**. بی‌سروصدا، بدون هیچ خطایی.
  ۵. **`which-group` نام دسته‌ها را نشان می‌داد** («میوه»،
     «حیوان») که هیچ تصویری ندارند. حالا هر گزینه نمونهٔ *دیگری*
     از همان دسته را تصویر می‌کند تا پاسخ لو نرود.
  ۶. **گِرد نیمه‌تصویری از گِرد تمام-متنی هم بدتر است.** در
     «گربه ۱ سگ ۱ اسب ۰ درخت ۱» تنها گزینهٔ بی‌تصویر خودش
     «فرق‌دار» به‌نظر می‌رسید و پاسخ را لو می‌داد.
  ۷. **چیدمان در دو سرِ طیف اندازه می‌شکست:** در ۳۲۰×۵۶۸ دکمهٔ
     «نقشهٔ سفر» و دکمهٔ «بعدیِ» صفحهٔ جشن زیر لبهٔ صفحه می‌افتادند
     (کودک اسکرول را کشف نمی‌کند)، و در گوشیِ بلند و تبلت کارت
     بازی حفره‌های ۱۴۰ تا ۲۳۶ پیکسلی دور خودش می‌ساخت.

  **قانون ۱۶ — هیچ گِردی نباید فقط با واژه پرسیده شود.** رقم و
  حرف فارسی مجازند (خودشان درس‌اند)، ولی واژهٔ فارسی به‌عنوان تنها
  نشانه یعنی گِرد برای مخاطب اصلی حل‌نشدنی است. استثنا فقط
  گِردهای خواندن و ترجمه است که *هدفشان* خواندن واژه است.
  `test-rounds.js` هر دو حالت (تمام-واژه‌ای و نیمه‌تصویری) را
  مکانیکی می‌گیرد.

---

## ۴. سیاست کیفیت

هر تغییر باید این‌ها را رد کند:

1. `npm test` — اعتبارسنجی محتوا + موتور گِرد
2. `npm run test:browser` — بازی واقعی در Chromium
3. **آزمون تکرار** — هیچ محتوای قالبی/تکراری
4. **آزمون ظاهری** — بدون سرریز/بریدگی در ۵ اندازهٔ صفحه
5. هر بررسی جدید باید **با تزریق خطا اثبات شود که قرمز می‌شود**

---

## ۵. وضعیت فعلی

| سنجه | حالا | هدف | وضعیت |
|------|------|-----|-------|
| درس | ۳۰۷ | ۳۰۵ | ✅ **هدف رد شد** |
| گِرد | ۱۸۱۹ | ۱۵۰۰+ | ✅ فراتر از هدف |
| نوع بازی | ۹۸ | ۲۵+ | ✅ فراتر از هدف |
| ترکیب آزموده | ۲۱۸٬۲۸۰ | — | ✅ همه سبز |
| حوزه | ۶ | ۶ | ✅ |
| شکل SVG | ۱۴۳ | ۶۰+ | ✅ فراتر از هدف |
| بازی آزاد | ۴ | ۴ | ✅ فاز ۷.۴ |
| کلیپ صوتی | ۷۹ | ۷۹ (ثابت) | ✅ قفل |
| پنل والدین | ✓ | ✓ | ✅ پشت دروازه |
| آفلاین کامل | ✓ | ✓ | ✅ ۴۵ فایل |
| بارگذاری | ۶۷ms | <۲۰۰۰ms | ✅ |
| نشان برنامه | ✓ | ✓ | ✅ پارامتری |
| باگ ظاهری شناخته‌شده | ۰ | ۰ | ✅ |

**۱۶٬۶۶۷ خط کد، ۳٫۸MB، بدون هیچ ابزار ساخت.**

### پیشرفت کلی پروژه

زیرساخت تمام است؛ آنچه مانده تقریباً همه‌اش محتواست.

| بخش | وزن | پیشرفت | سهم |
|---|---|---|---|
| معماری، موتور بازی، رابط، آفلاین، پنل والدین | ۴۰٪ | ۱۰۰٪ | ۴۰٪ |
| ده نگهبان خودکار + شکار باگ | ۱۵٪ | ۱۰۰٪ | ۱۵٪ |
| محتوا (۳۰۷ درس، ۱۸۳۱ گِرد، ۲۹ ساعت) | ۲۵٪ | ۱۰۰٪ | ۲۵٪ |
| زیباسازی (فاز ۷: صدا، آهنگ، انیمیشن، بازی‌ها، SVG، عمق داده) | ۱۰٪ | ۱۰۰٪ | ۱۰٪ |
| دسترس‌پذیری + آیکون | ۵٪ | ۱۰۰٪ | ۵٪ |
| بسته‌بندی اندروید + آزمون روی دستگاه واقعی | ۵٪ | ۰٪ | ۰٪ |
| **مجموع** | **۱۰۰٪** | | **≈۹۷٪** |

**یعنی حدود ۳٪ تا نقطهٔ تکمیل مانده — و بیشترش کاری است که در این
محیط انجام‌شدنی نیست:** بازگرداندن `android/`، ساخت APK (که ممنوع
است تا اجازهٔ صریح) و آزمون روی گوشیِ واقعی.
جزئیاتِ کاملِ آنچه مانده در **§۷.۱۰** آمده است.

> فاز ۷ کامل شد: ۷.۱ SVG، ۷.۲ انیمیشن، ۷.۳ صدا، ۷.۴ بازی‌ها،
> ۷.۶ عمق داده، ۷.۷ دسترس‌پذیری، ۷.۸ باگِ معنایی،
> **۷.۹ آهنگِ تولیدی و جرقه‌ها**.

**هر شش حوزه به هدف کمّی خود رسیده‌اند** — این نخستین بار است:

| حوزه | حالا | هدف |
|---|---|---|
| خواندن و نوشتن | ۱۰۲ | ۱۰۰ ✅ |
| ریاضی | ۶۰ | ۶۰ ✅ |
| تماشا و شناخت | ۴۰ | ۴۰ ✅ |
| انگلیسی | ۴۰ | ۴۰ ✅ |
| منطق و الگو | ۴۰ | ۴۰ ✅ |
| مهارت زندگی | ۲۵ | ۲۵ ✅ |

آنچه مانده دیگر نه محتواست و نه کیفیت: فقط بسته‌بندی اندروید و
آزمون روی دستگاه واقعی — و همان‌طور که قانون ۳ می‌گوید، ساخت APK
تا اجازهٔ صریح کاربر متوقف است.

**عمقِ محتوا (سنجهٔ §۷.۶):** ۲۹٬۲۷۳ نمای یکتا در شش حوزه — یعنی
کودکی که هر روز ده دقیقه بازی کند، ماه‌ها به صفحهٔ تکراری نمی‌رسد.

---

## ۶. آنچه مانده (به ترتیب اولویت)

همهٔ فازهای زیرساختی بسته شده‌اند. کارِ باقی‌مانده تقریباً همه‌اش
**مقیاس محتواست**، نه معماری.

### ۶.۱ — بزرگ‌کردن محتوا: ۲۰۶ → ۳۰۷ درس ✅ **تمام شد**

معیار همان است که در فاز ۳ آموختیم: **هر درس محتوای واقعی و آزموده**.
مولد باید هر چیزی را که نتواند صادقانه بسازد رد کند.

| حوزه | حالا | هدف | راه رسیدن |
|---|---|---|---|
| خواندن | ۱۰۲ | ۱۰۰ | ✅ **تمام** — ✅ دستهٔ اول (۲۰→۳۸): `SOUND_MAP` ۱۰۶→۱۹۲ واژه + **سازندهٔ درس تمرین**. ✅ دستهٔ دوم (۳۸→۴۲): **سازندهٔ درس مرور** با تکرار فاصله‌دار روی پنجرهٔ پنج نشانهٔ گذشته. ✅ دستهٔ سوم (۴۲→۴۵): **موتور جمله** (`src/data/sentences.js`، ۵۴ جمله) و سه گِرد `sentence-pic`/`pic-sentence`/`sentence-build`. ✅ دستهٔ چهارم (۴۵→۱۰۲): ۲۶ شکل تازه (واژهٔ خواندنیِ تصویردار ۳۴→۶۰ — سقف واقعیِ کل حوزه)، شش گِرد بر پایهٔ ترتیب رشدیِ Reading Rockets (`rhyme-pick` ۵ سال، `first-sound` ۵٫۵، `word-pic`/`pic-word` ۶، `word-build`/`which-sound` ۶٫۵)، سه گِرد درک مطلب (`wh-question`، `passage-read`، `passage-order`) و پنج خانوادهٔ درس تازه، از جمله «روان بخوان» که هر چهار لایه (صدا→کلمه→جمله→معنا) را در یک نشست می‌آورد. جمله‌ها ۵۴→۹۸. |
| ریاضی | ۶۰ | ۶۰ | ✅ **تمام** — ✅ دستهٔ اول (۸→۱۷): چوب‌خط، گوشه، تقارن، ارزش مکانی، شمارش چندتایی، جمعِ ناقص، نوشتن رقم. ✅ دستهٔ دوم (۱۷→۲۳): عددهای ۱۱–۱۹، محور اعداد، «بین»، اندازه‌گیری با واحد غیراستاندارد، عددهای ترتیبی. ✅ دستهٔ سوم (۲۳→۳۵): ترکیب‌های ده (K.OA.A.4) و مسئلهٔ کلامی (K.OA.A.2). ✅ دستهٔ چهارم (۳۵→۶۰): `doubles` (1.OA.C.6 — نخستین جمع‌های حفظی)، `fact-family` (1.OA.B.4 — تفریق = جمعِ ناتمام)، `count-tens` (K.CC.A.1) و ۲۵ درس تازه شامل اندازه‌گیری، ترتیب، شکل و مرور. |
| منطق | ۴۰ | ۴۰ | ✅ **تمام** — ✅ دستهٔ اول (۸→۱۴): منطقِ AND و NOT، مقایسه، توالی رویداد، علت و معلول. ✅ دستهٔ دوم (۱۴→۲۶): `pattern-make` خودزا (AB/AAB/ABB/ABBA/ABC) و `bigger-smaller` (ردیف‌بندی پیاژه). ✅ دستهٔ سوم (۲۶→۴۰): `and-rule` (منطقِ «و» — Funexpected آن را پس از NOT می‌گذارد)، `true-false` (سنجیدنِ گزاره به‌جای انتخاب) و `position` (استدلال فضایی، پیش‌نیاز هندسه). دو ویژگی تازه به `TRAITS` («در خانه»، «جانور») تا استخر دسته‌بندی تکراری نشود. |
| علوم | ۴۰ | ۴۰ | ✅ **تمام** — ✅ دستهٔ اول (۷→۱۳): چرخهٔ زندگی، شناوری، زنده/غیرزنده. ✅ دستهٔ دوم (۱۳→۲۵): سه محور NGSS کودکستان — آب‌وهوا (K-ESS2-1، K-ESS3-2)، نیازهای جانداران (K-LS1-1)، زیستگاه (K-ESS3-1). ✅ دستهٔ سوم (۲۵→۴۰): نیرو (K-PS2-1)، مواد، نور و سایه (K-PS3). هر پنج محور NGSS کودکستان پوشش دارد. |
| انگلیسی | ۴۰ | ۴۰ | ✅ **تمام** — ✅ دستهٔ اول (۶→۱۲): خانواده‌های CVC، واژه‌های دیداری، دوسویه‌سازی معنا. ✅ دستهٔ دوم (۱۲→۲۵): واژگان تصویری ۱۷→۵۱ از Cambridge YLE Pre-A1، شش درس موضوعی (حیوان، خوراکی، طبیعت، بدن، پوشیدنی، اشیا). ✅ دستهٔ سوم (۲۵→۴۰): خانواده‌های آوایی تازه، مرور موضوعی هر شش دسته، واژه‌های دیداری بیشتر. |
| مهارت زندگی | ۲۵ | ۲۵ | ✅ **تمام** — ✅ دستهٔ اول (۸→۲۰): `problem-solve` (تصمیم‌گیری مسئولانه) و `feeling-fix` (خودمدیریتی) از CASEL، به‌علاوهٔ مالکیت بدن از راهنمای بهزیستی. ✅ دستهٔ دوم (۲۰→۲۵): دوستی و نوبت، مدیریت خشم، ایمنی بیرون از خانه، همدلی و آزمون پایانی. |

**پیش‌نیاز هر درس تازه:** هر آیتمی که در گزینه‌ها می‌آید **باید شکل
SVG داشته باشد** — وگرنه `test-rounds.js` جلویش را می‌گیرد (قانون ۱۶).
پس رشد محتوا و رشد `svg.js` با هم پیش رفتند: ۳۶ → **۹۰** شکل.

**درسِ بزرگِ این فاز:** سقفِ واقعیِ حوزهٔ خواندن هرگز «تعداد درس»
نبود؛ **تعداد واژه‌هایی بود که هم با نشانه‌های آموخته‌شده خواندنی
باشند و هم تصویر داشته باشند** — فقط ۳۴ واژه از ۱۹۲. تا آن عدد به
۶۰ نرسید، هیچ‌کدام از نُه گِرد تازه ساخته نمی‌شد. یک محدودیتِ
داده‌ای می‌تواند کل یک حوزه را قفل کند و در هیچ آزمونی هم دیده
نشود، چون همه‌چیز «معتبر» است — فقط تولید نمی‌شود.

### ۶.۲ — کارهای فنی باقی‌مانده

- [ ] بازگرداندن پوشهٔ `android/` (کپسول Capacitor) — از تگ
      `v3.3.3-archive` قابل بازیابی است. **فقط در نقطهٔ تکمیل.**
- [ ] ساخت APK — **ممنوع تا اجازهٔ صریح کاربر.**
- [x] ~~دسترس‌پذیری~~ → §۷.۷، گاردِ `test-a11y.js`
- [x] ~~آیکون برنامه~~ → شش اندازه از یک منبع SVG
- [ ] آزمون روی دستگاه واقعی اندروید (لمس، اندازهٔ صفحه، سرعت)
      — **تنها کاری که در این محیط ممکن نیست.**

### ۶.۲.۱ — ده گارد، و اینکه هرکدام چه چیزی را نمی‌بیند

| گارد | زمان | می‌سنجد | **نمی‌بیند** |
|---|---|---|---|
| `validate` | ۱s | ساختار درس، پیوستگی مسیر | معنا |
| `test-rounds` | ۱۳s | ۲۱۹٬۷۲۰ ترکیب، ۹ قاعده | ظاهر |
| `test-browser` | ۲۰s | جریان واقعی کاربر | زیبایی |
| `test-delight` | ۳۰s | حس بازی، سرعت پاسخ | درستیِ محتوا |
| `test-visual` | ۷۴۰s | چهار اندازهٔ صفحه | معنای تصویر |
| `test-contrast` | ۲s | خوانایی ۱۴۷ تصویر | اینکه تصویر *چه* می‌گوید |
| `test-a11y` | ۹s | صفحه‌خوان، لمس، کنتراست | تجربهٔ کودک |
| `test-music` | ۱۴s | تمپو، بلندی، سکوتِ درس، نشتیِ اسیلاتور | **خوش‌آهنگ بودن** |
| `sync-sw` | ۱s | ۴۹ دارایی در کش | — |
| `build-icons --check` | ۱s | شش آیکون | — |

ستونِ آخر مهم‌ترین ستونِ این جدول است. §۷.۸ نشان داد هر نُه‌تا
می‌توانند سبز باشند و باگ در **نخستین صفحهٔ نخستین درس** بنشیند.

**و §۷.۱۱.۱ یک ستونِ دومِ نانوشته اضافه کرد: «به کجا نمی‌رسد؟»**
سرریزِ افقیِ بازی حافظه را `test-visual` *می‌سنجید* — سنجه‌اش درست
بود و کور نبود. فقط هرگز به حالتِ ۱۰ کارتی نمی‌رسید، چون آن حالت
چند مرحله بازیِ بُرده لازم دارد. گاردی که سنجهٔ درست دارد ولی به
حالتِ خراب نمی‌رسد، دقیقاً به‌اندازهٔ گاردِ کور بی‌فایده است — و
خطرناک‌تر، چون سبزش قانع‌کننده‌تر است.

و `test-music` صریح‌ترین نمونهٔ این ستون است: می‌تواند ثابت کند آهنگ
داخلِ درس ساکت است، اسیلاتوری رها نشده و تمپو در محدودهٔ کودک است —
ولی **هرگز نمی‌تواند بگوید آهنگ گوش‌نواز است یا رومخ.** آن را فقط
گوشِ آدم می‌فهمد. جایی که معیار عددی هست، چشم و گوش را کنار بگذار؛
جایی که نیست، آن‌ها تنها ابزارند.

### ۶.۳ — یادداشت‌های ثابت

- صداها **فقط ۷۹ تا** هستند و بیشتر نمی‌شوند (بدون TTS). محتوای جدید
  نباید به صدای ضبط‌نشده وابسته باشد؛ گفتار اختیاری است.
- `assets/audio/kid` دست‌نخورده بماند.
- **نسخه روی `1.0.0` قفل است.**

---

## ۷. فاز زیباسازی — «از درست، به دوست‌داشتنی»

> محتوا تمام شد و **درست** است. این فاز دربارهٔ چیز دیگری است:
> اینکه کودک *بخواهد* برگردد. برنامه‌ای که درست باشد ولی دوست‌داشتنی
> نباشد، در فروشگاه نمرهٔ سه می‌گیرد.

**قانون حاکم بر کل این فاز:** هر افزوده باید **صفر بایت دارایی**
اضافه کند. نه فایل صوتی، نه تصویر، نه کتابخانه. صداها ساخته
می‌شوند (Web Audio) و تصویرها SVG درون‌خط‌اند. دلیلش همان دلیل
همیشه است: برنامه باید کاملاً آفلاین و سبک بماند.

### ۷.۱ — SVG زیباتر: از «درست» به «جذاب»

امروز ۹۰ شکل داریم و همه *خوانا* هستند، ولی تخت‌اند: تک‌رنگ، بدون
عمق، بدون شخصیت. سه کار، به ترتیب اثر:

| # | کار | چرا | معیار پذیرش |
|---|---|---|---|
| ۱ | **سایهٔ نرم زیر هر شکل** — یک بیضیِ کم‌رنگ | شکل روی کارت «می‌نشیند» به‌جای اینکه شناور باشد | همان ۹۰ شکل، بدون تغییر در خوانایی |
| ۲ | **دو-تُنگی**: هر سطح رنگ روشن‌تر و تیره‌ترش را داشته باشد | حجم می‌سازد بی‌آنکه گرادیان لازم شود (گرادیان در SVG درون‌خط سنگین است) | هیچ شکلی بیش از ۴ رنگ نگیرد |
| ۳ | **چشم و برق**: هر جاندار یک نقطهٔ سفیدِ کوچک در چشم | همان یک نقطه، تصویر را از «نماد» به «شخصیت» می‌برد | فقط جانداران؛ اشیا نه |

**درسِ سه دورِ اصلاحِ چهره‌ها که همین‌جا ثبت می‌شود:** تن باید از
سر *بالاتر* شروع شود و سر رویش بنشیند — **همپوشانی، نه اتصال**.
گردنِ مستطیلیِ جدا همیشه لوله‌ای به‌نظر می‌رسد.

### ۷.۲ — انیمیشن: دوازده اصل، سه‌تایش کافی است

امروز ۱۲ `@keyframes` داریم و کار می‌کنند، ولی پراکنده‌اند. منبع
(Ripplix، IxDF، Excited) بر سه اصل تأکید دارد که بیشترین اثر را با
کمترین کار می‌دهند:

| اصل | کجا | زمان‌بندی (از منبع) |
|---|---|---|
| **Squash & Stretch** | دکمه هنگام لمس ۲–۳px فرو برود و برگردد | ۱۲۰–۲۰۰ms |
| **Anticipation** | پیش از هر گذارِ بزرگ، ۴۰–۸۰ms حرکتِ کوچکِ هم‌جهت | ۴۰–۸۰ms |
| **Follow-through / Stagger** | کارت‌های گزینه یکی‌یکی بیایند، نه همه با هم | ۴۰–۱۲۰ms فاصله |

قواعد سخت:
- **میکرو-تعامل ۱۲۰–۲۰۰ms، گذارِ صفحه ۲۵۰–۳۵۰ms.** کندتر = کودک
  منتظر می‌ماند؛ تندتر = نمی‌بیند چه شد.
- **`prefers-reduced-motion` باید محترم شمرده شود.** بعضی کودکان
  به حرکت حساس‌اند و این یک الزام دسترس‌پذیری است.
- **هیچ انیمیشنِ بی‌پایانی حین پرسش نیست** — قانون موجود، دست‌نخورده.
- انیمیشن روی `transform` و `opacity` بماند تا روی گوشیِ ارزان هم
  ۶۰fps بدهد.

### ۷.۳ — صدا و آهنگ: ساخته می‌شوند، ضبط نمی‌شوند

**این بخش قانون «هرگز صدای تازه» را نقض نمی‌کند.** آن قانون دربارهٔ
*گفتار* است (که TTS فارسی‌اش بد است و کلیپ‌هایش ۷۹ تا قفل شده).
صدای موسیقایی چیز دیگری است: با نوسان‌ساز ساخته می‌شود، **صفر بایت
فایل** دارد، و همین حالا هم `sfx.correct/wrong/tap/win` همین کار را
می‌کنند.

| کار | جزئیات |
|---|---|
| **گام پنج‌نتی (pentatonic)** | هر نتی از گام پنج‌نتی با هر نتِ دیگرش خوش‌آهنگ است. یعنی صدای «درست» می‌تواند هر بار نتِ *بعدی* را بزند و رشتهٔ پاسخ‌های درست خودش یک ملودیِ بالارونده بسازد — بدون آنکه هرگز ناکوک شود. |
| **پوشِ ADSR** | صدای فعلی خام است. یک `linearRampToValueAtTime` کوتاه در حمله و رهایش، صدا را از «بوق» به «نُت» تبدیل می‌کند. |
| **آهنگِ پایانِ درس** | چهار میله از همان گام، فقط وقتی درس تمام می‌شود. |
| **صدای متفاوت برای هر حوزه** | ریاضی زنگ‌دار، خواندن نرم — یک رنگ = یک معنی، این بار در صدا. |

⚠ همهٔ اینها پشت `muted` می‌مانند و برنامه باید **کاملاً بی‌صدا هم
کار کند** — قانون موجود.

### ۷.۴ — بخش «بازی‌ها»: جدا از مسیر درس

امروز همه‌چیز «درس» است: شش گِرد، یک ستاره، تمام. بازی چیز دیگری
است — **بی‌پایان، امتیازی، بدون درس‌نامه**. کودک وقتی خسته است
سراغش می‌رود، نه وقتی می‌خواهد یاد بگیرد.

معماری: بازی‌ها **از همان موتور گِرد** تغذیه می‌کنند (هیچ محتوای
تازه‌ای لازم نیست — ۹۸ نوع گِرد و ۲۱۸٬۲۸۰ ترکیب آماده است). فقط
قالبِ دورشان فرق می‌کند.

| بازی | مکانیک | از چه گِردهایی |
|---|---|---|
| **مسابقهٔ سرعت** | ۶۰ ثانیه، هر پاسخ درست +۱، غلط −۵ ثانیه | هر گِردِ گزینه‌ایِ سریع |
| **برجِ پاسخ** | هر پاسخ درست یک آجر؛ برج تا سقف صفحه بالا می‌رود | ریاضی و منطق |
| **حافظهٔ بزرگ** | همان `memory-pairs` ولی ۶→۸→۱۰ جفت، هر مرحله سخت‌تر | شکل‌ها |
| **جفت‌یابِ کلمه** | کلمه ↔ تصویر، با ساعت | `word-pic`/`pic-word` |

قواعد:
- **در دسترس، ولی نه سرِ راه.** یک دکمه در خانه، نه بخشِ اولِ صفحه.
  مسیر یادگیری باید مسیر پیش‌فرض بماند.
- **بدونِ اقتصادِ سکه.** قانون موجود. امتیاز فقط عددِ همان بازی است.
- بازی هم باید **بی‌صدا و بی‌خواندن** قابل انجام باشد.
- محدودیتِ زمانِ روزانه شامل بازی هم می‌شود.

**گسترش (۱۴۰۵/۰۶/۰۴، به درخواستِ مالک):** چهار بازی به هفت رسید — «سرعتِ شمارش» (شمارشِ خالص)، «طبیعت‌گرد» (مشاهدهٔ علمی) و «الگوی بعدی» (ادامهٔ الگو). همه از همان موتورِ گِرد، صفر محتوای تازه، زمان‌دارِ تک‌ضربه‌ای، بی‌صدا و بی‌خواندن. `test:games` می‌سنجد هر بازی واقعاً گِرد می‌سازد.

### ۷.۵ — ترتیب اجرا و وضعیت

| گام | وضعیت |
|---|---|
| **۷.۳ صدا** | ✅ **انجام شد** — گام پنج‌نتی، پوش ADSR، زنجیرهٔ بالارونده، رنگِ صوتیِ هر حوزه |
| **۷.۲ انیمیشن** | ✅ **انجام شد** — squash & stretch، anticipation، stagger ۵۵ms |
| **۷.۴ بخش بازی‌ها** | ✅ **انجام شد** — چهار بازی، از موتور موجود |
| **۷.۱ زیباسازی SVG** | ✅ **انجام شد** — کانتور یکدست روی ۹۵ شکل، گاردِ خوانایی |
| **۷.۶ عمق داده** | ✅ **انجام شد** — شش نوعِ چهار‌نمایی به ۱۰ تا ۱۵ رسیدند |
| **۷.۷ دسترس‌پذیری** | ✅ **انجام شد** — گاردِ هشتم؛ چهار رنگِ حوزه اصلاح شد |
| **۷.۸ باگ‌یابیِ بازی‌کردنی** | ✅ **انجام شد** — گاردِ نهم |
| **۷.۹ آهنگ و جرقه** | ✅ **انجام شد** — شش آهنگِ تولیدی، جشنِ دیداری، گاردِ دهم |

هر گام: گاردها سبز، عکس باز و *نگاه* شود، پوش.

**آنچه در اجرای ۷.۴ آموختیم — بازآراییِ ناگزیر:** بخش بازی‌ها
نمی‌توانست ساخته شود مگر آنکه `optionButton`، `buildStage`، `chip`،
`scatterSlots`، `orderView` و `memoryView` از دلِ `playScreen` بیرون
کشیده شوند. تا وقتی داخل آن بسته بودند، تنها راهِ استفادهٔ دوباره،
کپی کردنِ پنجاه خط بود — یعنی همان تکراری که قانون ۶ منعش می‌کند.
`orderView`/`memoryView` به شمارندهٔ `correct` درس وابسته بودند؛
به‌جای دست‌بردن در متغیرِ بیرونی، حالا یک callback به نام `onWin`
می‌گیرند و درس و بازی هرکدام کار خودشان را می‌کنند.

**و یک درس دربارهٔ خودِ گاردها:** گاردِ «خانه نباید بیش از ۲ دکمه
داشته باشد» با افزودن دکمهٔ «بازی‌ها» شکست. وسوسه این بود که عدد را
بالا ببرم و رد شوم. ولی قانونِ واقعیِ نقشهٔ راه **«یک تپ تا بازی»**
است، نه «دو دکمه» — و آن قانون نشکسته بود. پس آستانه به ۴ رفت (همان
بازهٔ پژوهشیِ ۳ تا ۵) و **یک آزمونِ سخت‌ترِ جایگزین** اضافه شد:
نخستین دکمهٔ خانه باید دکمهٔ بازی باشد. این یکی با تزریق خطا اثبات
شد. وقتی گاردی می‌شکند، اول بپرس «کدام قانونِ واقعی را محافظت
می‌کرد؟» — گاهی پاسخ این است که گارد باید *دقیق‌تر* شود، نه سست‌تر.

### ۷.۶ — عمقِ داده: معیاری که هیچ گاردی نمی‌سنجید

پرسشِ کاربر «پس یعنی محتوا تمام شده؟» یک سنجهٔ تازه ساخت. تا آن روز
همه‌چیز با **تعداد** سنجیده می‌شد: ۳۰۷ درس، ۱۸۳۱ گِرد، ۲۹ ساعت. اما
سؤال درست این است: **کودک چند نمای متفاوت می‌بیند؟**

اندازه‌گیری ساده بود — هر گِرد را ۲۵ بار بساز و کلیدِ یکتا
(پرسش + صحنه + برچسب و تصویرِ گزینه‌ها) بشمار:

| حوزه | نمای یکتا |
|---|---|
| خواندن | ۱۱٬۵۳۰ |
| انگلیسی | ۵٬۴۲۷ |
| ریاضی | ۴٬۷۳۹ |
| منطق | ۲٬۷۲۷ |
| علوم | ۲٬۵۸۶ |
| مهارت زندگی | ۱٬۲۳۴ |

سپس همان کار برای هر *نوع* گِرد، و مشکل بیرون افتاد: شش نوع دقیقاً
**۴ نما** داشتند — `season`، `push-pull`، `made-of`، `light-shadow`،
`what-if`. یعنی جدولِ داده‌شان به اندازهٔ تعداد گزینه بود، پس
**همهٔ داده در هر بار روی صفحه می‌آمد**. `season` در ده درس تکرار
می‌شد و هر ده بار همان چهار تصویر.

این دقیقاً همان اشتباهِ «کم‌تنوعیِ» بخش احساسات است، در لباسی تازه.
گاردِ کم‌تنوعی هم آن را نگرفت، چون آستانه‌اش «کمتر از ۳ نما» بود و
اینها ۴ نما داشتند — درست یک پله بالاتر از خط.

**درمان:** `FORCES` ۴→۱۲، `MATERIALS` ۴→۱۱، `LIGHT_FACTS` ۴→۱۰،
`SEASON_SIGNS` تازه با ۱۴ نشانه، و `what-if` ۴→۱۵ ردیف. نتیجه: هر
شش نوع اکنون بین ۱۰ تا ۱۵ نما دارند.

دو تصمیمِ ریز که ارزش ثبت دارند:

- **`SEASONS` دست‌نخورده ماند.** وسوسه این بود که همان جدول بزرگ
  شود، ولی چرخهٔ فصل‌ها به دقیقاً چهار ردیف نیاز دارد. یک جدول
  نمی‌تواند هم‌زمان «چهار فصلِ سال» و «نشانه‌های فراوان» باشد.
  دو کاربرد ⇒ دو جدول.
- **`what-if` بدونِ یک بایت دارایی تازه سه‌برابر شد**، چون
  صحنه‌های `SITUATIONS` و `SCENES` از قبل کشیده شده بودند و این
  گِرد از آن‌ها استفاده نمی‌کرد. پیش از کشیدنِ شکل تازه، همیشه
  ببین چه چیزی بی‌استفاده مانده.

همچنین درس‌های زیر پنج گِرد از **۱۱ به ۵** رسیدند: خانوادهٔ «صدای
گم‌شده» دو گِردِ سبک‌تر (`first-sound`، `rhyme-pick`) در انتها گرفت
تا درس با آسان‌تر تمام شود. پنج درسِ باقی‌مانده قابل دفاع‌اند: سه
درسِ حافظه هر گِردشان یک بازیِ کامل است و هر پنج درس ۶ دقیقه‌اند —
حجمشان درست است، فقط شمارِ گِردشان کم به نظر می‌رسد.

**درسِ روش‌شناختی:** «تمام شد؟» را هرگز با شمارش پاسخ نده. عددِ کل
می‌تواند بزرگ باشد در حالی که تجربهٔ کودک تکراری است. برای هر سنجهٔ
کمّی، سنجهٔ کیفیِ متناظرش را پیدا کن.

**و گارد را سخت‌تر کن، نه فقط داده را بیشتر.** آستانهٔ کم‌تنوعی از
عددِ دستیِ ۳ به `AGE_TRACKS.school.optionCount + 1` رفت — چون قانونِ
واقعی این است که «پولِ داده باید از تعداد گزینه بزرگ‌تر باشد، وگرنه
انتخاب توهم است». همان لحظه چهار موردِ تازه گیر افتاد که هیچ‌کس
ندیده بود:

| گِرد | پیش | پس |
|---|---|---|
| `event-order` | ۳ زنجیره | ۸ زنجیره |
| `safety-order` | ۳ زنجیره | ۶ زنجیره |
| `feeling-fix` | ۴ نما | ۷ احساس، ۱۵ حرکت |
| `subitize` | ۳ نما | مثبتِ کاذب — `max` به فهرست قفل‌شده‌ها رفت |

`feeling-fix` همان اشتباهِ آشنا را پنهان کرده بود: «شاد» و «آرام»
چهره داشتند ولی هیچ حرکتی نداشتند، پس همیشه بدل بودند و هرگز
آموخته نمی‌شدند. نکتهٔ ظریف این بود که حرکتِ درست برای حالِ خوب
«آرام کردن» نیست — «قسمت کردن» و «یاد گرفتن» است. وقتی داده‌ای جا
افتاده، اول بپرس چرا؛ گاهی مدلِ ذهنیِ جدول اشتباه است، نه داده‌اش.

و `subitize` یادآوری کرد که هر شکستِ گارد، باگ نیست: `max: 3` یعنی
درس عمداً فقط ۱ و ۲ و ۳ نقطه نشان می‌دهد، چون هدفش «تشخیص فوریِ
مقدار کوچک بدون شمردن» است. سه نما آنجا محدودیتِ آموزشی است، نه
تنبلیِ داده. گاردِ خوب باید بتواند این دو را از هم جدا کند.

### ۷.۱ — زیباسازی SVG: از «درست» به «دیده‌شدنی» ✅

کار با یک فرض شروع شد که غلط بود. فکر می‌کردم این فاز دربارهٔ
*سلیقه* است: سایهٔ نرم، سایه‌روشن، برقِ چشم. منبع اصلی همان اول
هشدار داد که برای تصویرِ کوچک عکسش درست است — «هرچه تصویر کوچک‌تر
و کاربردی‌تر، مسطح‌تر بهتر؛ سایه در تصویر کوچک فقط گِل‌آلودش
می‌کند». تصویرهای ما در دکمهٔ گزینه حدود ۱۰۰px‌اند.

پس به‌جای سایه‌پردازی، **اندازه گرفتم**: هر ۹۵ شکل را روی کارت
سفیدِ دکمه رندر کردم و دو عدد گرفتم — چند درصدِ کادر جوهرِ واقعی
دارد، و کنتراستِ میانگین نسبت به سفید چقدر است.

نتیجه یک باگ بود، نه یک سلیقه:

| شکل | پوششِ جوهر |
|---|---|
| **ماست** | **۰٫۰٪** |
| **ابر** | **۰٫۰٪** |
| خرگوش | ۱٫۶٪ |
| شیر | ۱٫۱٪ |
| مرغ | ۳٫۱٪ |

صفر درصد یعنی **هیچ پیکسلی تیره‌تر از L=200 وجود ندارد**: کودک
روی دکمه یک کارتِ سفیدِ خالی می‌دید. و هر پنج گارد سبز بودند، چون
همه «تصویر وجود دارد» را می‌سنجیدند نه «تصویر دیده می‌شود».

علتِ مشترک ساده بود: شکل‌های کم‌رنگ **کانتور نداشتند**. «ابر» فقط
چند دایرهٔ `#BFD9E8` بود روی سفید. درمان هم ساده بود و همان چیزی
شد که فاز ۷.۱ واقعاً باید می‌بود: کانتورِ تیره‌تر از فیل، یک
سطحِ روشن‌تر برای سمتِ نور، و برقِ چشم در چهره‌ها. یعنی «Flat 2.0»
با دستِ سبک، نه سایه‌پردازیِ کامل.

**گاردِ تازه — `scripts/test-contrast.js`:** پوششِ جوهر ≥۳٫۵٪ و
کنتراست ≥۱٫۲۸. آستانه‌ها از داده آمدند نه از سلیقه: پس از اصلاح،
ضعیف‌ترین شکلِ پذیرفتنی «اردک» بود با ۱۰٪. با تزریق یک شکلِ محو
اثبات شد که واقعاً می‌شکند.

**دو یافتهٔ جانبی که ارزششان از خودِ فاز بیشتر بود:**

**۱. «موم» عملاً «کاغذ» بود.** هر دو مستطیلِ کرم‌رنگ با لبهٔ موجی.
بدتر: تنها کاربردِ موم گِردِ «شمع از چه ساخته شده؟» است و در همان
گِرد «کاغذ» یکی از بدل‌هاست — یعنی دو گزینهٔ تقریباً یکسان روی یک
صفحه. موم به کندوی عسل تغییر کرد؛ شش‌ضلعی‌های زرد در هیچ شکل دیگری
نیست. **دو شکل نباید فقط از روی برچسبشان قابل تفکیک باشند.**

**۲. «ماه» تنها شکلی بود که `id` داشت** (`mask id="mn"`). دو ماه در
یک صفحه — که در گِردِ نور و سایه رخ می‌دهد — یعنی دو عنصر با شناسهٔ
یکسان و مرورگر اولی را به هر دو می‌دهد. با یک path هلالی بازنویسی
شد. قاعده: **در SVGهای درون‌خطیِ تکرارشونده هرگز `id` نگذار.**

### ۷.۱.۱ — باگی که خودم ساختم و درسِ فارسی‌اش

برای افزودن کانتور به «خانه» یک regex نوشتم که نامِ شکل را می‌گرفت.
**«رودخانه» به «خانه» ختم می‌شود**، پس regex اول به رودخانه خورد و
رودخانه تبدیل به خانه شد — یک درسِ علوم و یک نشانهٔ فصل خراب شدند.

هر شش گارد سبز ماندند: شکل وجود داشت، کانتور داشت، کنتراستش خوب
بود، صفحه جابه‌جا نشده بود. فقط اتفاقی چشمم به خروجی افتاد.

**درسِ فارسی‌اش را جدا نگه دار:** نامِ شکل‌ها در فارسی پسوندِ هم‌اند
— خانه/رودخانه، برگ/بزرگ، آب/آبی، شیر (نوشیدنی)/شیره. جست‌وجوی نام
**همیشه** باید مرزِ ابتدای خط داشته باشد (`\n  نام:`)، نه صرفاً خودِ
نام.

**و گاردِ دومی که از آن زاده شد:** `test-contrast.js` حالا هر جفت
شکلِ *دقیقاً یکسان* را هم گزارش می‌کند. با تزریقِ همان باگِ واقعی
اثبات شد. این الگوی همیشگیِ پروژه است: **هر باگی که از دستِ گاردها
در رفت، باید گاردِ تازه‌اش را بزاید** — وگرنه دوباره می‌آید.

### ۷.۱.۲ — گاردی که فقط دو‌سومِ کار را می‌دید

`test-contrast.js` را که نوشتم، روی `SHAPE_NAMES` بستمش: ۹۵ شکل.
ولی برنامه **۱۴۸** تصویر دارد. چهره‌ها، موقعیت‌ها، صحنه‌ها، خطرها و
گام‌های ایمنی — ۵۳ تصویر — بیرونِ دیدِ گارد بودند. و اتفاقاً همان‌ها
بیشترین بازکشیدن را در کل پروژه داشته‌اند.

با گسترشِ گارد به همهٔ جدول‌ها، بلافاصله یکی گیر افتاد: **«خواب» با
۰٫۷٪ پوششِ جوهر.** علتش هم آموزنده بود — این شکل خوابیدن را با
`<text>Z</text>` نشان می‌داد. سه ایراد در یک تصمیم: کم‌رنگ، وابسته
به فونتِ سیستم، و **حرفِ لاتین در برنامه‌ای برای کودکِ فارسی‌زبانِ
پیش‌خوان**. با تخت و پتو و چشمِ بسته و سه حبابِ بالارونده بازکشیده
شد؛ حباب هیچ زبانی ندارد.

اکنون هیچ `<text>` در کل `svg.js` نیست.

**درس:** وقتی گاردی می‌نویسی، اولین سؤال این نیست که «چه چیزی را
می‌سنجد؟» بلکه **«چه چیزی را نمی‌سنجد؟»** دامنهٔ ناقص از نبودِ گارد
خطرناک‌تر است، چون سبز بودنش اطمینانِ کاذب می‌دهد.

### ۷.۷ — دسترس‌پذیری: رنگ‌هایی که هفت ماه غلط بودند

گاردِ هشتم (`scripts/test-a11y.js`) پنج چیز را می‌سنجد: نامِ دکمه
برای صفحه‌خوان، هدفِ لمس ≥۴۴px، کنتراستِ متن، دیده‌شدنِ حلقهٔ فوکوس،
و `dir="ltr"` روی متنِ لاتین.

دامنه‌اش عمداً محدود است. چک‌لیستِ عمومیِ WCAG اینجا گمراه‌کننده
می‌شد: در برنامه‌ای که کاربرش کودکِ پیش‌خوان است، «متنِ جایگزینِ
تصویر» معنای دیگری دارد — تصویر *خودِ پرسش* است، نه تزئینِ متن. پس
همهٔ SVGها `aria-hidden`اند و برچسبِ متنیِ دکمه کار را می‌کند.

اولین اجرا یک چیزِ ناخوشایند پیدا کرد:

| حوزه | رنگِ قدیم | کنتراستِ متنِ سفید |
|---|---|---|
| مهارت زندگی | `#E08A1E` | **۲٫۶۸** |
| علوم | `#4CAF50` | **۲٫۷۸** |
| خواندن | `#E4572E` | **۳٫۶۸** |
| ریاضی | `#2E86AB` | **۴٫۱۱** |

**چهار رنگ از شش زیر حدِ ۴٫۵ بودند** — یعنی عنوانِ درس روی دکمهٔ
بزرگِ خانه، بزرگ‌ترین عنصرِ کلِ برنامه، از ابتدای پروژه بدخوان بود.
هیچ‌کدام از هفت گاردِ دیگر این را نمی‌دید، چون همه ساختار را
می‌سنجیدند نه رنگ را.

وسوسه این بود که متن تیره شود. دو دلیل ردش کرد: متنِ تیره روی
نارنجیِ اشباع هم بدخوان است، و قانونِ «یک رنگ = یک معنا» می‌شکست
(سفید همه‌جای برنامه یعنی «متنِ روی رنگِ حوزه»). پس خودِ رنگ‌ها یک
درجه تیره‌تر شدند — همان فام، همان هویت، کنتراستِ استاندارد:

`#C8461F` ۴٫۸۳ · `#256E8C` ۵٫۶۹ · `#2F7C3E` ۵٫۱۶ · `#A8620C` ۴٫۷۵

سپس صفحهٔ خانه و نقشه رندر و *نگاه* شدند تا مطمئن شوم رنگِ تیره‌تر
برنامه را غمگین نکرده. نشده بود؛ دایره‌های نقشه هنوز شاد و متمایزند.

**درس:** کنتراست را نمی‌شود با چشم قضاوت کرد. `#E08A1E` روی سفید
«خوانا به نظر می‌رسد» و ریاضیاتش می‌گوید نیست. هر جا معیارِ عددی
وجود دارد، چشم را کنار بگذار — و هر جا معیارِ عددی وجود *ندارد*
(مثل «آیا هنوز شاد است؟») چشم تنها ابزار است.

### ۷.۸ — باگی که فقط با *بازی کردن* پیدا شد

پس از سبز شدنِ هشت گارد، یک درس را در مرورگر باز کردم و عکس گرفتم.
نخستین صفحهٔ نخستین درسِ کلِ برنامه این بود:

> **کدام یکی حرف «الف» است؟**  ← و تصویرِ یک **انار**

منطقِ کد درست بود: کودک صدای «الف» را می‌شنود و انار لنگرِ تصویری
است. ولی قانونِ بنیادیِ خودِ پروژه می‌گوید **برنامه باید بدون هیچ
صدایی کامل کار کند** — و بی‌صدا، آن صفحه یک انارِ بی‌ربط کنارِ یک
پرسشِ حرفی بود. کودک نمی‌فهمید انار آنجا چه می‌کند.

اصلاحِ اول را هم غلط نوشتم: «انار با کدام حرف شروع می‌شود؟» —
بلافاصله معلوم شد `LETTER_PICTURE` جدولِ *شروع* نیست. «ب» → «ابر»،
«ن» → «خانه»، «ع» → «شمع». این عمدی است، چون برای هر ۳۲ حرف کلمهٔ
آغازینِ تصویردار وجود ندارد. پرسشِ درست: **«کدام حرف در ابر هست؟»**

**گاردِ تازه در `test-rounds.js`:** حرفِ پاسخ باید در نامِ تصویر
وجود داشته باشد. ۵۶ گِرد بررسی، صفر ناهمخوانی.

**درس:** هشت گاردِ خودکار سبز بودند و باگ در **نخستین صفحهٔ نخستین
درس** نشسته بود. گارد ساختار را می‌سنجد؛ *معنا* را باید دید. هیچ
مقدار آزمونِ خودکار جای «برنامه را باز کن و بازی کن» را نمی‌گیرد.

---

### ۷.۹ — آهنگ: وقتی پژوهش خواستهٔ کاربر را نقض می‌کند

خواستهٔ روشن بود: «چند آهنگ طولانی با ریتم جذاب، پخشِ تصادفی».
پیش از نوشتنِ کد، دو پژوهش خوانده شد و یکی از آن‌ها نتیجه را وارونه
کرد:

- **musicscience.net:** تمپوی محبوبِ کودک ≈**۱۵۰ BPM** است (بزرگ‌سال
  ۱۰۰–۱۲۰) و با بالا رفتنِ سن کند می‌شود. پس «ریتمِ جذاب» یعنی تند.
- **ScienceDirect، EEG روی کودکان ۵ تا ۷ ساله حینِ کتاب‌خوانی:** در
  اتاقِ ساکت، **هوشیاری بدونِ موسیقیِ پس‌زمینه به‌طور معنادار بالاتر
  است.** تفاوت فقط وقتی از بین می‌رود که نویزِ مزاحم وجود داشته باشد.
- **Springer، ۴ تا ۶ ساله، توجهِ اجرایی:** موسیقیِ **کند** کاراییِ
  پردازش را حفظ کرد؛ موسیقیِ **تند** نکرد.
- **راهنمای سنی:** برای ۶ تا ۹ سال فقط سازی، **بدون کلام** — کلام با
  همان کانالِ زبانی رقابت می‌کند که کودک برای خواندن لازم دارد.

یعنی «آهنگِ تند و شاد» دقیقاً همان چیزی است که **نباید** هنگامِ درس
پخش شود. اگر خواسته را لفظ‌به‌لفظ اجرا می‌کردم، برنامه جذاب‌تر و
آموزشی‌تر *به‌نظر* می‌رسید و در عمل یادگیری را کمی بدتر می‌کرد.

**راهِ حل، نه رد کردنِ خواسته و نه اجرای کورش:**

| کجا | آهنگ |
|---|---|
| خانه و نقشهٔ سفر | آرام، ۸۸–۱۰۴ BPM |
| بخش بازی‌ها و بازیِ زمان‌دار | شاد، ۱۳۸–۱۵۲ BPM |
| **داخلِ درس** | **هیچ** |

و پیش‌فرض **خاموش**، پشتِ دروازهٔ والدین. چون قانونِ برنامه می‌گوید
همه‌چیز باید بی‌صدا هم کامل کار کند؛ آهنگ افزودنی است نه لازمه.

**«طولانی» چگونه حل شد بدون یک بایت دارایی.** نوشتنِ چند دقیقه
موسیقیِ نُت‌به‌نُت هزاران خط می‌شد و بعد از دو بار شنیدن تکراری. به‌جایش
هر آهنگ یک *دستورِ ساخت* است: پیش‌رَویِ آکورد + گام + چگالیِ ملودی.
ملودی از **پنتاتونیکِ ماژور** تصادفی انتخاب می‌شود — گامی که هر
ترکیبش خوش‌آهنگ است — پس آهنگ **بی‌پایان است و هرگز عیناً تکرار
نمی‌شود.** شش آهنگ در `src/core/music.js` جمعاً چند کیلوبایت متن‌اند.

**زمان‌بندی.** نُت‌ها هرگز با `setTimeout` پخش نمی‌شوند (روشِ
«دو ساعت» در web.dev): یک تایمرِ ۲۵ms فقط *نگاه* می‌کند و هر نُتی را
که در ۱۲۰ms آینده سررسید دارد با ساعتِ دقیقِ Web Audio زمان‌بندی
می‌کند. تایمر می‌تواند بلرزد؛ صدا نمی‌لرزد.

**یک تصمیمِ معماری که ارزشِ تکرار دارد.** انتخابِ آهنگ در **یک
نقطه** انجام می‌شود: تابعِ `render`. هر صفحه فقط *اعلام* می‌کند چه
حالی دارد (`data-music="calm"`) و پیش‌فرضِ نبودِ این نشان، **سکوت**
است. یعنی هر صفحهٔ تازه‌ای که در آینده اضافه شود، به‌صورتِ خودکار
ساکت است و کسی نمی‌تواند سهواً وسطِ درس آهنگ بگذارد. **ایمنی از
جنسِ پیش‌فرض، نه از جنسِ یادآوری.**

### ۷.۹.۱ — جرقه‌ها، و چیزی که فقط اسکرین‌شات نشان داد

قانونِ «هرجا صدا نیست، راهنمای دیداری باشد» تا امروز در لحظهٔ پاسخِ
درست ناقص بود: صدای خوش وجود داشت، ولی برای کودکی که صدا را خاموش
کرده تنها جشن، یک انیمیشنِ کوچک روی خودِ دکمه بود.
`src/core/sparkle.js` چهارده ذرهٔ رنگی می‌پاشد — بدون canvas (برای
۱۴ ذره توجیه ندارد) و **بدون قرمز**، چون قرمز در این برنامه معنیِ
«نادرست» دارد و «هرگز ضربدرِ قرمز» یک تصمیمِ ثبت‌شده است.

**و بعد عکس گرفته شد.** نسخهٔ اول با شعاعِ ۴۶px ذره‌ها را درست
**روی حرفِ پاسخ** می‌نشاند: در اسکرین‌شات، «ا» زیرِ کنفتی گم شده بود.
هیچ آزمونی این را نمی‌گرفت — تعدادِ ذره‌ها درست بود، انیمیشن اجرا
می‌شد، خطایی نبود. شعاع به ۷۴–۱۲۰px رفت و ذره‌ها از همان ابتدا با
۳۴٪ فاصله شروع می‌شوند تا مرکزِ کارت همیشه خالی بماند.

> **جشن نباید چیزی را که جشنش گرفته شده پنهان کند.**
> این جمله را هیچ گاردی نمی‌تواند بسنجد.

### ۷.۹.۲ — یک قاعدهٔ CSS، دو باگ، و اعتباری که گارد پس گرفت

صفحهٔ تنظیمات پس از افزودنِ کلیدِ آهنگ عکس‌برداری شد و **دو** خطا
نشان داد که هیچ‌کدام در کدِ تازه نبودند:

۱. **کلید ۳۵۸px پهن شده بود و از لبهٔ صفحه بیرون زده بود** (x = −۷۱).
۲. **کادرِ «اسم کودک» فلشِ کشویی داشت** — ورودی‌ای که وعدهٔ فهرست
   می‌داد و فهرستی نداشت.

هر دو یک ریشه داشتند: قاعده‌ای که `.field input` و `.field select` را
با هم جمع کرده بود. `width: 100%` کلید را کش داد و `background-image`
فلش را به کادرِ متنی هم داد. قاعده شکسته شد و عرض به
`input[type='text']` محدود گشت.

**و نکتهٔ صادقانه:** در یادداشتِ اولِ همین بخش نوشته بودم «فقط
اسکرین‌شات نشانش داد». **غلط بود.** `test-visual` که هم‌زمان در
پس‌زمینه می‌دوید، مستقلاً همان را در **هر چهار اندازهٔ صفحه** گرفت:
«سرریز افقی ۴۶۱ > ۳۹۰» و «هدف لمسی کوچک — switch ۳۵۸×۴۰».

این وارونهٔ درسِ §۷.۸ است و به همان اندازه مهم: آنجا آموختیم گارد
معنا را نمی‌بیند، **اینجا آموختیم چشم هم گاهی دیرتر از گارد است.**
گارد در چهار اندازه دید، من در یکی. هیچ‌کدام جای دیگری را نمی‌گیرد —
و ادعای «گارد این را نمی‌دید» باید *بررسی* شود، نه فرض.

**و باری سوم، همان گارد باز هم برنده شد.** پس از اصلاحِ عرض، کلید
۶۸×۴۰ شد و `test-visual` دوباره قرمز داد: **حداقلِ هدفِ لمسی ۵۶px
است.** وسوسه روشن بود — آستانه را به ۴۰ بیاور، چون کلیدِ کوچک
خوش‌ترکیب‌تر است. ولی ۵۶ از پژوهش آمده و این کلید را والد با شست
می‌زند. **کلید بزرگ شد (۸۸×۵۶)، آستانه دست‌نخورده ماند.**
همان قاعدهٔ §۷.۵: وقتی گاردی می‌شکند، اول بپرس کدام قانونِ واقعی را
محافظت می‌کرد.

### ۷.۱۱ — سه کارِ باقی‌مانده، و باگی که زیرشان پنهان بود ✅

هر سه کارِ فهرستِ §۷.۱۰ بسته شد. ولی نکتهٔ این نشست خودِ آن سه کار
نیست؛ چیزی است که هنگامِ انجامشان بیرون افتاد.

**۱. پنج شکلِ کم‌جوهر — و یکی که اصلاً باگ بود.**
قاشق، شمع، کتاب‌خواندن و نفس‌عمیق فقط کم‌رنگ بودند: پس‌زمینهٔ روشن روی
کارتِ سفید. اما **چاقو** یک باگِ ساختاری داشت — دسته با یک
`transform="rotate(42 …)"` جدا از تیغه می‌افتاد و تصویر می‌شد «یک
تیغه، و یک مستطیلِ بنفشِ شناور در گوشه». نگهبانِ کنتراست آن را
۵٫۵٪ پوشش گزارش می‌کرد، یعنی *در آستانه، ولی قبول*. عدد قبول بود؛
تصویر غلط بود.

> **درس:** نگهبانِ کنتراست می‌گوید «چیزی آنجا هست». نمی‌گوید «آن چیز
> یک شیءِ به‌هم‌پیوسته است». بین این دو، فاصله‌ای هست که فقط چشم
> پرش می‌کند.

**۲. صدای لمس — و تلهٔ نامِ کلاس.**
بیست دکمهٔ ناوبری بازخوردِ صوتی نداشتند. راهِ آشکار (یک `sfx.tap()`
در هر بیست جا) هم تکرار بود و هم تضمینِ فراموش‌شدنِ دکمهٔ بیست‌ویکم.
به‌جایش یک شنوندهٔ سراسری در مرحلهٔ capture روی ریشه نشست، با فهرستی
از نواحیِ مستثنا (گزینه‌های پاسخ، که صدای معنادارِ خودشان را دارند).

و بلافاصله خودم در همان فهرست اشتباه کردم: `order-row` و `order-bank`
نوشتم، در حالی که نامِ واقعی `order-slots` و `order-tray` بود. **هیچ
چیز نمی‌شکست** — فقط صدای لمس روی صدای «آفرین» می‌افتاد و کسی هرگز
نمی‌فهمید چرا صدا گِل‌آلود است. یک نگهبانِ تازه در `test-delight`
اضافه شد که وجودِ هر کلاسِ مستثنا را در رابط بررسی می‌کند، و با
برگرداندنِ همان اشتباه اثبات شد.

> **درس:** رشتهٔ متنی که به کدِ فایلِ دیگری اشاره می‌کند، یک وابستگیِ
> بدونِ بررسی‌کننده است. کامپایلر نمی‌بیندش، آزمون نمی‌بیندش، فقط
> کاربر می‌بیندش — آن هم مبهم.

**۳. صفحهٔ «دربارهٔ برنامه» — همهٔ اعداد زنده.**
هیچ عددی دستی نوشته نشد: ۳۰۷ درس، ۱۸۳۱ تمرین، ۲۹ ساعت و شمارشِ
هر حوزه در لحظه از `LESSONS` شمرده می‌شوند. عددِ دستی در پروژه‌ای که
هفته‌ای دو بار محتوا اضافه می‌شود، ظرفِ چند روز دروغ می‌شود — و
صفحهٔ «دربارهٔ ما» بدترین جای ممکن برای دروغ است.

### ۷.۱۱.۱ — باگی که ده نگهبان ندیدند: بازی حافظه

هنگامِ بازکردنِ چهار بازی برای *نگاه کردن* (نه آزمودن)، جدولِ حافظه
شش کارت را ۴+۲ چیده بود: چهار کارت در ردیفِ اول، دو تا در دوم، و یک
فضای خالیِ بزرگ کنارشان که کودک آن را «کارتِ گمشده» می‌بیند.

دنبالش که رفتم، زیرِ آن یک باگِ جدی‌تر بود: **در حالتِ ۱۰ کارتی،
صفحه در عرضِ ۳۲۰ پیکسل تا ۴۳۶ پیکسل پهن می‌شد** — کلِ برنامه افقی
می‌لغزید.

سه علتِ روی‌هم‌افتاده:

| علت | چرا خاموش بود |
|---|---|
| `repeat(auto-fit, …)` | برای تعدادِ نامعلوم خوب است؛ اینجا فقط ۴ حالت داریم (۶/۸/۱۰/۱۲) و چیدمان باید صریح باشد |
| `1fr` | در واقع `minmax(auto, 1fr)` است و آن `auto` یعنی «از محتوایم کوچک‌تر نشو» → کفِ نامرئیِ عرض. `minmax(0, 1fr)` برش می‌دارد |
| `min-height: var(--tap)` + `aspect-ratio: 1` | کفِ *ارتفاع* را به کفِ *عرض* ترجمه می‌کند. ۵ کارتِ ۷۶ پیکسلی در ۳۲۰ پیکسل جا نمی‌شود |

و مهم‌ترین بخش: **نگهبانِ بصری سرریزِ افقی را می‌سنجید.** کور نبود.
فقط هرگز به آن صفحه *نرسیده بود*، چون رسیدن به مرحلهٔ ۱۰ کارتی
نیازمندِ چند بار بردنِ بازیِ حافظه است. نگهبان هر بار در مرحلهٔ ۱
متوقف می‌شد و سبز می‌داد.

> **درس — نسخهٔ تازه‌ای از یک درسِ قدیمی:** تا حالا می‌دانستیم «نگهبان
> فقط چیزی را می‌بیند که سنجیدنش را یادش داده‌ای». این مورد یک پله
> جلوتر است: **نگهبان می‌تواند سنجهٔ درست را داشته باشد و باز هم کور
> باشد، چون به آن حالت از برنامه نمی‌رسد.** پرسشِ دوم کنارِ پرسشِ اول
> می‌نشیند: «چه چیزی را نمی‌سنجد؟» و **«به کجا نمی‌رسد؟»**

اصلاح: `memoryCols()` صریح (۶→۳×۲، ۸→۴×۲، ۱۰→۵×۲، ۱۲→۴×۳)، و
نگهبانِ بصری حالا هر چهار حالت را مستقیم می‌سازد و اندازه می‌گیرد
به‌جای آنکه امید داشته باشد در بازی به آن‌ها برسد.

### ۷.۱۳ — دادهٔ مرده، و فضایی که هیچ‌کس نمی‌شمرد

این نشست هیچ ویژگیِ تازه‌ای اضافه نکرد. سه چیزِ *موجود* را درست کرد،
و هر سه یک الگوی مشترک دارند: **چیزی که هست ولی کار نمی‌کند، هیچ
خطایی نمی‌دهد.**

**۱. `curriculum.icon` — هفت ماه دادهٔ مرده.**
هر حوزه یک فیلدِ `icon` داشت: `book`، `numbers`، `leaf`، `abc`،
`heart`، `puzzle`. هیچ‌کدام هرگز خوانده نمی‌شد. در همان حال،
بزرگ‌ترین عنصرِ صفحهٔ خانه یک کارتِ ۲۵۰ پیکسلی بود که **فقط متن**
داشت — برای کودکِ پنج‌ساله‌ای که خواندن بلد نیست، یعنی «یک مستطیلِ
نارنجی».

قانونِ برنامه («هرجا صدا نیست، راهنمای دیداری باشد») درست روی
پرمصرف‌ترین صفحه نقض شده بود، و علتش این بود که کسی حدس زده بود
نشان‌ها «هستند» — چون فیلدشان بود.

> **درس:** دادهٔ خوانده‌نشده بدتر از دادهٔ نبوده است. نبودنش را کسی
> می‌بیند؛ بودنِ بی‌مصرفش این توهم را می‌سازد که کار انجام شده.
> حالا `validate` هر دو سو را می‌گیرد: نشانِ ناموجود = خطا، نشانِ
> بی‌صاحب = هشدار.

**۲. نشانی که «ili» خوانده می‌شد.**
طرحِ اولِ نشانِ ریاضی سه میلهٔ عمودی با دو نقطه بود — چوب‌خطِ شمارش.
در بزرگ‌نمایی معلوم شد چشم آن را **`ili`** می‌خواند: سه حرفِ لاتین،
در برنامه‌ای فارسی، برای کودکی که تازه الفبا یاد می‌گیرد. قانونِ
ثبت‌شدهٔ «در نشان نه رقم و نه حرفِ لاتین» را نقض می‌کرد.

هیچ نگهبانی نگرفتش و هیچ‌وقت هم نخواهد گرفت: **نگهبان‌ها نمی‌خوانند.**
چُرتکه جایگزین شد.

**۳. ۴۶٪ صفحه که هیچ‌کس نمی‌شمرد.**
`test-visual` سرریز و بریدگی را می‌سنجید — یعنی «چیزی بیرون زده».
عکسش را نمی‌دید. اندازه‌گیری نشان داد در گوشیِ ۴۳۰×۹۳۲:

| | پیش | پس |
|---|---|---|
| فضای بی‌استفادهٔ زیرِ گزینه‌ها | ۴۲۷px (۴۶٪) | ۸۴px (۹٪) |
| ارتفاعِ صحنه | ۱۶۰px | ۵۰۳px |
| تصویرِ پرسش | ۱۲۰px | ۲۲۴px |
| نسبتِ تصویر به کارت | ۲۴٪ | ۴۴٪ |

علت: `.screen` فلکسِ ستونی بود ولی **هیچ فرزندی `flex-grow` نداشت**،
پس همه بالا جمع می‌شدند و فضای اضافه زیرشان می‌افتاد. یک `flex: 1`
روی صحنه.

و در همان اندازه‌گیری یک باگِ قدیمی هم بیرون افتاد: در ۳۲۰×۵۶۸ صفحه
**۲۵ پیکسل سرریزِ عمودی** داشت. دقیقاً آن‌قدر کوچک که کسی شک نکند و
آن‌قدر بزرگ که گزینه‌ها زیرِ لبه بروند. `min-height: 130px` کفِ سختی
می‌ساخت؛ با `clamp(90px, 16vh, 130px)` برطرف شد.

> **درس:** «چیزی بیرون نزده» با «چیدمان درست است» یکی نیست. صفحه‌ای
> که نصفش خالی است باگِ ظاهری است، حتی وقتی هیچ چیز نشکسته. نگهبانِ
> تازه‌ای اضافه شد که فضای بی‌استفادهٔ زیرِ گزینه‌ها را می‌شمارد و
> بالای ۲۸٪ قرمز می‌شود — با CSS قدیمی ۳۷٪ می‌گرفت و رد می‌شد.

### ۷.۱۳.۱ — سه بار که «عدد سبز شد ولی صفحه بدتر شد»

نگهبانِ «فضای بی‌استفاده» را که نوشتم، بلافاصله سه صفحهٔ دیگر را
گرفت که خودم ندیده بودم. اصلاحشان سه اشتباهِ آموزنده داشت:

**۱. نگهبان را فریب دادم — بی‌آنکه بخواهم.**
بسیاری از گِردها اصلاً `.stage` ندارند (پرسش تصویری است و خودِ
گزینه‌ها تصویر دارند). برای آن‌ها `align-content: center` گذاشتم.
عدد از ۵۲٪ به ۲۶٪ افتاد و سبز شد. **ولی صفحه بدتر شده بود:** فضای
خالی از زیرِ دکمه‌ها به *بالای سرشان* منتقل شده بود و دکمه‌ها یک
نوارِ معلق در میانهٔ سفیدی بودند. نگهبان فقط زیر را می‌شمرد.

> اگر معیارت را بهینه کنی به‌جای چیزی که معیار *نماینده‌اش* بود،
> عدد بهتر می‌شود و محصول بدتر. **بعد از هر اصلاح، عکس را نگاه کن —
> مخصوصاً وقتی عدد سبز شده.**

**۲. باگ را در یک اندازه دیدم و فقط همان‌جا درست کردم.**
دکمهٔ بلند برچسب را از تصویرش جدا می‌کرد. در تبلت دیدمش، برای تبلت
مهارش کردم، سبز شد. بعد همان سنجه را در بقیهٔ اندازه‌ها گرفتم:
گوشیِ بزرگ ۱۰۷px و متوسط ۹۵px همان مشکل را داشتند.

> **وقتی باگی را در یک اندازه دیدی، همان سنجه را در همهٔ اندازه‌ها
> بگیر.** وگرنه فقط جایی را که نگاه کرده‌ای درست می‌کنی.

**۳. عددی را دست‌کاری کردم که در معادله نبود.**
افزودنِ نشانِ حوزه، کارتِ خانه را در ۳۲۰×۵۶۸ سرریز کرد. `min-height`
کارت را از ۱۹۰ به `clamp(150px…)` بردم. **جواب نداد و نباید هم
می‌داد** — کارت `flex: 1 1 auto` دارد و دارد فضا را می‌بلعد، پس
اندازه‌اش را کف تعیین نمی‌کند.

و وقتی علتِ واقعی را پیدا کردم، یک بلوکِ `@media` نوشتم که
`buddy-row` را پنهان کند. آن هم کار نکرد: **همان قاعده از پیش وجود
داشت**، در `@media (max-height: 520px)`، فقط آستانه‌اش پایین بود. و
چون قاعدهٔ موجود پایین‌ترِ فایل بود، بر قاعدهٔ من غلبه می‌کرد.

> دو درس در یک باگ: **علت را پیدا کن، بعد عدد را عوض کن** — نه
> برعکس. و **پیش از افزودنِ قاعدهٔ CSS، `grep` کن ببین همان قاعده
> جای دیگری هست یا نه.** دو قاعدهٔ هم‌معنی یعنی یکی همیشه بی‌اثر است.

نتیجهٔ نهایی، در پنج اندازه:

| اندازه | فضای هدررفته | سرریز | فاصلهٔ تصویر تا برچسب |
|---|---|---|---|
| ۳۲۰×۵۶۸ | ۱۵٪ | ندارد | ۱۶px |
| ۳۶۰×۶۴۰ | — | ندارد | ۱۶px |
| ۳۹۰×۸۴۴ | ۱۰٪ | ندارد | ۱۶px |
| ۴۳۰×۹۳۲ | ۹٪ | ندارد | ۱۶px |
| ۷۶۸×۱۰۲۴ | ۸٪ | ندارد | ۱۶px |

### ۷.۱۴ — چقدر تا تکمیل مانده؟

**وضعیت: ≈۹۸٪ از کارِ نرم‌افزاری.** آنچه مانده در دو دسته است و
**دستهٔ دوم اصلاً کارِ کدنویسی نیست.**

**الف) کارِ باقی‌ماندهٔ کد — تقریباً هیچ:**

هر سه کارِ فهرستِ پیشین بسته شد. آنچه می‌ماند نگهداری است، نه ساخت:
اگر محتوایی اضافه شود، نگهبان‌ها باید سبز بمانند.

**ب) کارِ انتشار — نیازمندِ اجازهٔ صریحِ شما:**

| کار | وضعیت |
|---|---|
| بازگرداندنِ `android/` از تگ `v3.3.3-archive` | **آماده نیست — سه ناسازگاری، §۷.۱۵** |
| ساختِ APK/AAB و امضا | **ممنوع تا نقطهٔ تکمیل — طبق دستور شما** |
| آزمون روی گوشیِ واقعی | تنها آزمونی که هیچ شبیه‌سازی جایش را نمی‌گیرد |

### ۷.۱۵ — «آماده» نبود: سه ناسازگاریِ اندروید

این سند چند نشست پیاپی نوشته بود «بازگرداندنِ `android/` از تگ:
آماده، انجام‌نشده». هنگامِ آماده‌سازیِ مخزن برای تحویل، تگ را واقعاً
باز کردم و بررسی کردم. **۴۵ فایل سالم آنجاست و `appId` هم درست است
(`ir.parvareshhoosh.app`)، ولی «آماده» غلط بود.** سه چیز نمی‌خوانند:

| # | ناسازگاری | چرا مهم است |
|---|---|---|
| ۱ | `capacitor.config.json` می‌گوید `"webDir": "www"` — **پوشهٔ `www` وجود ندارد** | ساختارِ نسخهٔ ۴ فایل‌ها را در ریشه دارد (`index.html`، `src/`، `assets/`). یا باید مرحلهٔ کپی به `www` ساخته شود، یا `webDir` به `"."` تغییر کند — و دومی خطرناک است چون `node_modules` و `scripts/` را هم داخلِ APK می‌برد. **راهِ درست: یک اسکریپتِ `build:www` که فقط داراییِ لازم را کپی کند.** |
| ۲ | `android/app/build.gradle` روی `versionName "3.3.3"` و `versionCode 10` است | نسخهٔ برنامه `1.0.0` قفل است (قانون ۱). این‌ها باید هم‌راستا شوند — ولی ⚠ `versionCode` در کافه‌بازار **هرگز نباید کم شود**؛ اگر نسخهٔ ۳.۳.۳ قبلاً منتشر شده، `versionCode` باید از ۱۰ بالاتر برود حتی اگر `versionName` پایین بیاید. این تصمیمِ مالکِ پروژه است، نه تصمیمِ فنی. |
| ۳ | `package.json` فعلی **هیچ وابستگیِ Capacitor ندارد** | تنها وابستگیِ پروژه `playwright-core` است. برای ساخت باید `@capacitor/core` و `@capacitor/cli` و `@capacitor/android` نصب شوند — که حجمِ `node_modules` را چند برابر می‌کند. عمداً حذف شده بودند تا مخزن سبک بماند. |

نکتهٔ مثبت: `variables.gradle` مدرن است (`compileSdk 35`, `targetSdk 35`,
`minSdk 23`) و `AndroidManifest.xml` هم `supportsRtl="true"` و هم
`usesCleartextTraffic="false"` دارد — یعنی برای برنامهٔ فارسیِ آفلاین
درست تنظیم شده و نیازی به بازنویسی ندارد.

> **درسِ روش‌شناختی:** «آماده، انجام‌نشده» در یک سند، یک **ادعای
> بررسی‌نشده** است. سه نشست پیاپی آن سطر را کپی کردم بدون آنکه یک بار
> `git show` بزنم. همان خانوادهٔ خطاست که در کد «گاردِ سبز» می‌نامیمش:
> چیزی که *به‌نظر* تمام است چون کسی نگاهش نکرده. **هر سطرِ «آماده» در
> این سند باید یا شواهدِ کنارش باشد، یا به «بررسی‌نشده» تغییر کند.**

**ج) کارِ غیرِفنی — بیرون از دستِ من:**

توضیحِ فروشگاه، تصاویرِ صفحهٔ بازار، سیاستِ حریمِ خصوصی، حسابِ
توسعه‌دهندهٔ کافه‌بازار، و قیمت‌گذاری. هیچ‌کدام کدنویسی نیستند.

**پاسخِ صریح:** از نظرِ نرم‌افزاری، **یک تا دو نشستِ کاری** تا نقطه‌ای
که بگویم «آماده است». گلوگاهِ واقعی پس از آن، **آزمون روی دستگاهِ
واقعی** است — و آن نیازمندِ لغوِ ممنوعیتِ ساختِ APK از سوی شماست.

⚠ **آن ۲٪ باقی‌مانده صادقانه‌ترین بخشِ این سند است:** هجده گارد و ۲۹٬۲۷۳
نمای متمایز ثابت می‌کنند برنامه *خراب نیست*. هیچ‌کدام ثابت نمی‌کنند
کودکِ هفت‌سالهٔ واقعی بعد از بیست دقیقه هنوز مشتاق است. آن عدد را
فقط یک کودک و یک گوشی می‌سازند.

### ۷.۱۶ — زیبایی و انیمیشن: قطارِ کارِ باقی‌مانده

**۱۴۰۵/۰۶/۰۴ — به درخواستِ مالک.** تا امروز «ارتقای کیفیتِ هنری» یک
نکتهٔ کیفیِ کلی بود (§۰.۴). این بخش آن را به **هشت کارِ مشخص**
تبدیل می‌کند تا ایجنتِ تازه بدونِ سلیقه‌بازی اجرایش کند.

⚠ **این فهرست از خواندنِ کد جمع شده، نه از بازی‌کردن.** هر ادعا مسیرِ
فایل دارد. پیش از شروعِ هر آیتم، خودت آن صفحه را بازی کن — قضاوتت
از صفحهٔ زنده، مقدم بر این فهرست است.

#### الف) آنچه امروز هست — تکرارِ آن ممنوع

| چیز | شاهد در کد |
|---|---|
| ۱۶ انیمیشنِ `@keyframes` | `src/styles/main.css`: ورودِ صفحه (`screen-in`)، ورودِ کارت‌ها با stagger ۵۵ms (`pop`/`stage-in`)، درست (`cheer`)، غلط (`shake`)، ستاره‌های پیاپی (`award-in`)، جرقه (`sparkle-fly`)، درخششِ نوارِ پیشرفت (`sheen`)، تپِ زمانِ کمِ بازی (`tick`) و… |
| جرقهٔ ۱۴ذره‌ای بدونِ canvas | `src/core/sparkle.js` — در چهار نقطه: پاسخِ درست، جاشماری، جفت‌یابی، جایگذاری |
| `prefers-reduced-motion` | هم در CSS (سطر ۱۴۷۳) هم در `sparkle.js` — محترم شمرده می‌شود |
| هوشی با ۴ حالتِ معنادار | `src/core/buddy.js` — neutral/happy/encourage/think + قوانینِ سخت (پایین) |
| راهنمای خودکار بعد از مکث | `screens.js` — با تأخیرِ متناسب با سن (`hintDelayMs`) |
| زنجیرهٔ نُت‌های بالارونده | گامِ پنج‌نتی (§۷.۳) — **فقط صدا؛ نمایشِ دیداری ندارد** |

**جمع‌بندی:** پایهٔ «درست» کامل است. آنچه نیست «دوست‌داشتنی» است — و
**یک شکافِ واقعی:** زنجیرهٔ پاسخ‌های درست در حالتِ بی‌صدا هیچ نشانهٔ
دیداری ندارد (قانونِ «بی‌صدا باید کار کند» را ناقض می‌کند).

#### ب) قوانینی که هیچ آیتمی نمی‌تواند بشکند

1. **هیچ انیمیشنِ بی‌پایانی حینِ پرسش** — `test:delight` مکانیکی می‌سنجد.
2. حرکت فقط روی `transform`/`opacity` — گوشیِ ارزان هم ۶۰fps.
3. میکرو ۱۲۰–۲۰۰ms، گذارِ صفحه ۲۵۰–۳۵۰ms.
4. `prefers-reduced-motion` همیشه محترم.
5. قوانینِ هوشی: حینِ پرسش حرکت نمی‌کند، بی‌پایان نیست، حالتِ تزئینی ندارد.
6. **بی‌صدا هم باید جشن باشد** — هر شادیِ تازه باید در سکوت دیده شود.
7. پس‌زمینه **ایستا** مجاز، **زنده** ممنوع (فاز ۴.۵: بارِ شناختیِ بی‌دلیل).

#### ج) هشت کار — به ترتیبِ اثر

| # | کار | شاهدِ خلأ | نقطهٔ تماس |
|---|---|---|---|
| ۱ | ✅ **زنجیرهٔ دیداریِ پاسخ‌های درست** — سه نشانِ کوچک کنار هوشی؛ هر پاسخِ درست یکی را روشن می‌کند، خطا همه را خاموش. روشن‌شدن در *لحظهٔ گذار* است (بعد از پاسخ)، پس قانونِ ۱ نمی‌شکند. | ملودیِ بالارونده فقط صدا دارد؛ بی‌صدا هیچ نشانه‌ای نیست | `screens.js` (playScreen)، `main.css` |
| ۲ | ✅ **لرزشِ مهربان** — `navigator.vibrate(15)` فقط روی پاسخِ درست؛ با feature-detect و پشتِ تنظیمِ والدین. روی دسکتاپ بی‌اثر است. | در کلِ `src/` صفر ارجاع به `vibrate` | `screens.js`، تنظیمات |
| ۳ | ✅ **نقشهٔ سفرِ صحنه‌دار** — یک راهِ پیوستهٔ SVG پشتِ فهرست و مرزِ فصل‌ها با رنگِ حوزه. ⚠ ۳۰۷ آیتم یک‌جا رندر می‌شود؛ یک `path` پس‌زمینه اضافه کنید نه ۳۰۷ گرافِ تازه. | الان ۳۰۷ سطرِ سطریِ `map-item` است؛ کودک «سفر» را نمی‌بیند | `screens.js` (mapScreen)، `svg.js` |
| ۴ | ✅ **جشنِ قدم‌های گرد و پایانِ فصل** — قدم‌های ۵۰/۱۰۰/۲۰۰/۳۰۷ و پایانِ هر حوزه، جشنِ متفاوت از درسِ عادی. | هر ۳۰۷ درس جشنِ یکسان دارند | `screens.js` (doneScreen) |
| ۵ | ✅ **پس‌زمینهٔ صحنه‌ایِ ایستا** — تپه/ابر/خورشیدِ ساده برای خانه و جشن؛ بدونِ هیچ حرکتی. | پس‌زمینه‌ها رنگِ تخت‌اند | `main.css` |
| ۶ | ✅ **هوشی: حالت‌های معنادارِ تازه** — فقط اگر معنا داشته باشند؛ مثلاً «wow» برای قدمِ گرد. قانون: حالتِ تزئینی نداریم. | فقط ۴ حالت است | `buddy.js` |
| ۷ | 🔄 **ارتقای هنریِ SVG** — نشستِ ۱ از چند (۱۲ از ۱۴۷ تصویر) — هایلایت، سایهٔ زمینِ نرم، پالتِ غنی‌تر؛ تدریجی: ۱۰ تا ۱۵ تصویر در هر نشست، هر بار `test:contrast` سبز. دو تصویر نباید فقط با برچسب فرق کنند. | ۱۴۷ تصویر تخت و یکنواخت‌اند (نمونه: گربه = بیضی + سبیل) | `svg.js` |
| ۸ | ✅ **کارتِ صحنه‌دار برای هر بازی** | `game-card`ها فقط آیکون + متن‌اند | `screens.js` (gamesScreen) |

#### د) ترتیِ اجرا و تعریفِ «تمام»

**۱ و ۲ در یک نشست** (کوچک‌اند و آیتمِ ۱ یک شکافِ قانونی است نه
سلیقه) → **۳** (بیشترین اثرِ دیداری) → **۴** → **۵ و ۶** → **۷ در
چند نشستِ پیاپی** → **۸** آخر.

هر آیتم «تمام» است وقتی:
1. هر هجده گارد سبز است (به‌علاوهٔ گاردِ تازهٔ خودش).
2. گاردِ تازه **منفی‌آزمون** شده و مطمئن شده‌اید تزریق واقعاً اعمال شده.
3. از صفحه عکس گرفته‌شده و *دیده* شده — نه فقط DOM سنجیده.
4. کامیتِ فارسی + پوش.

> **یادآوریِ سخت‌گیری:** هیچ‌کدام از این هشت کار اجازهٔ ساختِ APK
> نمی‌دهند — قانون ۳ سرِ جایش است.

**وضعیت (۱۴۰۵/۰۶/۰۴):** آیتم‌های ۱ تا ۶ و ۸ ✅ تمام شدند — زنجیرهٔ
دیداری در `lesson-head` (آیتم ۱)، لرزشِ مهربان در `src/core/haptics.js`
با سوییچِ «لرزشِ مهربان» (آیتم ۲)، راهِ پیوستهٔ نقشه با نشانِ رنگیِ
هر حوزه (آیتم ۳)، جشنِ قدم‌های ۵۰/۱۰۰/۲۰۰/۳۰۷ و پایانِ حوزه با
`milestoneOf` (آیتم ۴)، پس‌زمینهٔ صحنه‌ایِ ایستا برای خانه و جشن
(آیتم ۵)، حالتِ «وای»ِ هوشی (آیتم ۶)، کارت‌های صحنه‌دارِ بازی
(آیتم ۸). گاردهای تازه: `test:streak`، `test:haptic`، `test:map`،
`test:milestone`، `test:scene`، `test:games`، `test:art`.

**آیتم ۷ (ارتقای هنریِ SVG) در جریان است — نشستِ ۱ از چند:** ۱۲
تصویر ارتقا یافتند (گربه، سگ، ماهی، سیب، موز، گل، درخت، توپ، کتاب،
خانه، ماشین، ابر) با سایهٔ زمینِ نرم، هایلایت و لهجهٔ پالت. فهرستِ
ارتقایافته در `scripts/test-art.js` می‌ماند و با هر نشست بزرگ‌تر
می‌شود؛ ۱۳۵ تصویرِ دیگر مانده.

**گسترشِ بازی‌ها (دستورِ مالک، در چارچوبِ §۷.۴):** چهار بازی به
هفت رسید — «سرعتِ شمارش»، «طبیعت‌گرد» و «الگوی بعدی» اضافه شدند؛
همه از موتورِ گِرد، صفر محتوای تازه، زمان‌دارِ تک‌ضربه‌ای و
بی‌خواندن. `test:games` می‌سنجد هر بازی واقعاً گِرد می‌سازد.
فیلدِ مردهٔ `about` از بازی‌ها حذف شد (هیچ‌جا رندر نمی‌شد).

## ۸. پنج درسِ روش‌شناختیِ این پروژه

اگر همه‌چیز فراموش شود، این پنج تا بماند:

**۱. آزمون سبز یعنی «آنچه سنجیده‌ام سالم است»، نه «برنامه سالم است».**
هر پنج آزمون سبز بودند در حالی که کودک می‌توانست پیشرفتش را پاک کند،
نمودار به والد دروغ می‌گفت، بازی تصویری متنی شده بود و یک دکمه اصلاً
برچسب نداشت. **عکس صفحه را باز کن و نگاه کن.**

**۲. هر محافظ تازه باید منفی-آزمون شود** — باگی که قرار است بگیرد را
عمداً برگردان و ببین قرمز می‌شود *و پیام خوانا می‌دهد*. محافظِ ترتیب
صداکشی سه بار نوشته شد و هر سه بار منفی-آزمون نشان داد کور است.

**۳. کلیدی که رابط نمی‌شناسد، بی‌سروصدا نادیده گرفته می‌شود.**
`pattern-next` کلید `shape` می‌فرستاد و `screens.js` هرگز نشنیده بودش:
نه خطا، نه شکست آزمون — فقط واژه به‌جای تصویر. **وقتی کلید تازه‌ای
اضافه می‌کنی، شاخهٔ مصرف‌کننده‌اش را در `screens.js` بگرد.**

**۵. نمایشِ تازه پاسخ را لو می‌دهد، مگر خلافش ثابت شود.**
سه بار پشت سر هم: `mirror` شکل و بازتابش را کنار هم گذاشت؛ محور
اعداد نقطهٔ فرود را نارنجی کرد؛ صف ترتیبی نشانهٔ شروع نداشت (که
عکسِ همان است — بدون سرنخ، پرسش حدس می‌شود). هر سه از آزمون‌ها رد
شدند. **برای هر نمایش بپرس: کودک چه چیزی را باید در ذهنش بسازد که
روی صفحه نیست؟ اگر پاسخی نداری، گِرد بی‌ارزش است.**

**۴. داده می‌تواند با خودش جور باشد و با تصویر جور نباشد.**
«گوشه‌ها را بشمار» برای ستاره پاسخ ۵ را درست می‌دانست، ولی نقشِ SVG ده
رأس دارد. عدد با عدد می‌خواند، پس هیچ آزمونی قرمز نشد؛ فقط کودکی که
*واقعاً می‌شمرد* ۱۰ می‌گرفت و «اشتباه» می‌شد. یعنی درس همان مهارتی را
که می‌آموزاند تنبیه می‌کرد. **برای هر گِردی که دربارهٔ یک تصویر پرسش
می‌کند، پاسخ را از روی خودِ تصویر بشمار، نه از روی جدول.**
