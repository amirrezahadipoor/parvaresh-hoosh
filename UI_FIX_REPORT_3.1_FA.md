# گزارش رفع باگ‌های ظاهری (نسخه ۳.۱)

بسته `parvaresh-hoosh-3.1-age-ordered.zip` جایگزین کل فایل‌های قبلی مخزن شد و سپس اپ روی مرورگر واقعی
(Chromium، ابعاد گوشی ۴۱۲×۹۱۵ و ۳۶۰×۶۴۰) اجرا و باگ‌یابی شد.

## ۱) شکست تست‌ها و توقف ساخت APK
`test_runtime.js` عدد ثابت `710` را انتظار داشت، ولی محتوای نسخهٔ ۳.۱ به **۱۴۶۰ راند** رسیده بود.
در نتیجه `npm test` شکست می‌خورد و ورک‌فلو **قبل از ساخت APK** متوقف می‌شد.
اکنون به‌جای عدد ثابت، کف ۷۰۰ راند بررسی می‌شود تا با رشد محتوا نشکند.

## ۲) فضای خالی ۴۲۶ پیکسلی در همهٔ درس‌ها
قانون `.activity-visual-stage { flex: 0 0 auto }` در انتهای CSS، رشد ناحیهٔ تصویر را می‌گرفت و
`margin-top:auto` روی دکمه‌ها یک شکاف **۴۲۶ پیکسلی** بین سؤال و گزینه‌ها باز می‌کرد.
اصلاح شد به `flex: 1 1 auto` و `margin-top: 0` → فاصله به **۱۲ پیکسل** رسید.

## ۳) کوچک ماندن تصویرها
تصاویر SVG صفت‌های ثابت (مثل `95x95`) دارند و با `height:auto` در فضای آزادشده ریز می‌ماندند.
با `height:100%` و سقف `min(38vh, 260px)` حالا تا **۲۶۰ پیکسل** بزرگ می‌شوند.

## ۴) هدر رفتن نیمی از صفحه در شهربازی
`.arcade-card` با ارتفاع طبیعی خود می‌ایستاد و در هر ۶ بازی بین **۳۶۵ تا ۵۱۲ پیکسل** فضای مرده
زیر کارت می‌ماند. با کشیده‌شدن کارت و صحنه، اتلاف به **۲۴ پیکسل** کاهش یافت.

## ۵) نامرئی بودن بادکنک‌ها
انیمیشن بادکنک‌ها برای صحنهٔ ۳۸۰ پیکسلی ثابت کدنویسی شده بود؛ در صحنهٔ بلندتر بادکنک‌ها
هیچ‌وقت وارد کادر نمی‌شدند (**۰ از ۳ قابل مشاهده**). مسیر حرکت حالا از ارتفاع واقعی صحنه
محاسبه می‌شود → **۷ از ۷ بادکنک** دیده می‌شوند.

## وضعیت نهایی
- `npm test`: موفق (۱۴۶۰ راند رندر شد)
- ۲۴ راند از ۶ حوزهٔ درسی: بدون سرریز، بدون دکمهٔ خارج از کادر، بدون اسکرول ناخواسته
- خطای کنسول: ۰ — درخواست ناموفق: ۰

---

# بازرسی ریزبینانه (مرحلهٔ دوم)

هر ۱۴۶۰ راندِ تولیدشده از هر ۲۹۲ درس در مرورگر واقعی رندر و اندازه‌گیری شد
(سرریز افقی، دکمهٔ خارج از کادر، اندازهٔ ناحیهٔ لمس، خطای کنسول).

## ۶) صفت نامعتبر در SVG (خطای واقعی کنسول)
`SvgArt.object` دو ورودی می‌گیرد `(kind, size)` ولی در `generator.js` سه ورودی پاس داده شده بود:
`object('star', '#F9CA24', 110)`. در نتیجه کد رنگ در جای «اندازه» می‌نشست و مرورگر
صفت `width` را رد می‌کرد. تنها موردِ ناهماهنگی ورودی در کل پروژه بود (بقیه بررسی و سالم بودند).

## ۷) سرریز کارت‌های «ترتیب‌گذاری» از دو لبهٔ صفحه
کارت‌ها یک SVG ثابت ۸۵ پیکسلی دارند و چون `min-width` پیش‌فرض فلکس `auto` است،
اجازهٔ کوچک شدن نداشتند. در ردیف‌های ۴ و ۵ تایی، کارت‌ها **از هر دو لبهٔ صفحه بیرون می‌زدند**
(۲۹ راند از ۲۰۷). با `min-width:0` و مقیاس‌پذیر کردن تصویر → **۰ مورد**.

## ۸) ریز بودن پالت رنگ نقاشی
۱۲ دایرهٔ رنگ فقط **۲۷ پیکسل** و دکمه‌های ابزار هم کوچک بودند — برای انگشت کودک نامناسب.
به ۴۰ پیکسل رسیدند (۷۸ راند نقاشی اصلاح شد).

## ۹) شکست دوبارهٔ CI به‌خاطر لینتر
اسکریپت `audit_assets.js` با regex ساده دنبال `width="#..."` می‌گردد و **متن توضیحات کد**
را هم کد حساب می‌کرد؛ توضیحی که برای مورد ۶ نوشته بودم باعث خروج با کد ۱ می‌شد.
متن توضیح بازنویسی شد.

## مواردی که بررسی شد و سالم بود (باگ نبودند)
- **دکمهٔ راهنمای حافظه**: در اندازه‌گیری ۳۴.۶ پیکسل دیده می‌شد، ولی علتش انیمیشن ورود
  `scale(0.96)` بود؛ اندازهٔ واقعی دقیقاً **۳۶ پیکسل** است.
- **علامت‌گذاری پاسخ غلط**: کلاس `wrong` بعد از ۶۰۰ میلی‌ثانیه پاک می‌شود (طراحی درست).
- **پایان درس**: کارت تبریک، کانفتی، ۳ ستاره و ذخیرهٔ پیشرفت همگی سالم‌اند.
- **ذخیره‌سازی**: نام کودک، ستاره‌ها و درس‌های تمام‌شده بعد از ریلود باقی می‌مانند.
- **قفل والدین**: معادلهٔ ریاضی و صفحه‌کلید ۱۳ کلیدی درست کار می‌کند.

## وضعیت نهایی
| بخش | نتیجه |
|---|---|
| ۱۴۶۰ راند رندر | ۰ شکست |
| سرریز افقی | ۰ |
| دکمهٔ خارج از صفحه | ۰ |
| ناحیهٔ لمس کوچک | ۰ |
| خطای کنسول | ۰ |
| `npm test` | موفق |

---

# مرحلهٔ سوم: اصلاح محتوا، صدا و تجربهٔ کودک

## ۱۰) دسته‌بندی‌های تقلبی — درس‌ها محتوای مربوط به خودشان را نشان نمی‌دادند
بزرگ‌ترین ایراد محتوایی. درس «صدای الفبا: اَ - ب - پ - ت» دربارهٔ حرف «گ» و «ش» سؤال می‌پرسید،
چون `roleFactory` با `pick(ALPHABET)` یک حرف **تصادفی از کل الفبا** برمی‌داشت و عنوان درس را
نادیده می‌گرفت. حالا عنوان هر درس تجزیه می‌شود و فقط حروف خودش تدریس می‌شوند.
**نتیجه: ۲۸ سؤال نامربوط از ۳۲ → صفر.**

## ۱۱) تصویرهایی که واقعاً نقاشی نشده بودند
تابع `enrichRound` به **هر گزینه** به‌زور یک تصویر می‌چسباند؛ برای واژه‌هایی که نقاشی نداشتند
یک «کاشی متنی» (مستطیل رنگی با نوشتهٔ همان کلمه) می‌ساخت و آن را جای تصویر جا می‌زد —
حتی برای عبارت‌هایی مثل «داد زدن در خانه».
حالا فقط واژه‌هایی که **نقاشی واقعی** دارند تصویر می‌گیرند و بقیه متن تمیز می‌مانند.
**نتیجه: تصاویر تقلبی از ۴۹٪ → ۶٪** (باقی‌مانده کاشی حرف/عدد است که درست است).

## ۱۲) سؤال‌هایی که جواب را لو می‌دادند
- «این تصویر مربوط به کدام کلمه است؟» تصویری نشان می‌داد که **خود کلمه رویش نوشته شده بود**.
- معماها («ابزاری برای تمیز نگه داشتن دندان‌ها») تصویر جواب را نمایش می‌دادند.
- «عدد ۲ کدام است؟» کارت عدد ۲ را کنارش می‌گذاشت.
هر سه اصلاح شد (معما بدون تصویر، عدد با «تعداد اشیاء» به‌جای رقم).
**سؤال‌های خودلودهنده: ۹۵ → ۱۵** (بقیه «کدام عدد بزرگ‌تر است؟» است که نمایش دو عدد لازمهٔ سؤال است).

## ۱۳) صدای فارسی کار نمی‌کرد
`speechSynthesis` با `lang='fa-IR'` استفاده شده بود، ولی **WebView اندروید هیچ صدای فارسی ندارد**
→ در عمل روی گوشی کاملاً بی‌صدا بود. روش عوض شد: **۸ فایل صوتی واقعی فارسی**
با هوش مصنوعی ساخته و داخل اپ بسته‌بندی شد (`assets/audio/`)، برای هر ۸ درس الفبا.
هنگام ورود به درس **خودکار پخش می‌شود** و آفلاین هم کار می‌کند (در Service Worker کش شده).

## ۱۴) دادهٔ تحلیل کودک از صفر شروع نمی‌شد
هر حوزه با نمرهٔ ساختگی **۶۵** شروع می‌شد و کف نمره ۴۰ بود؛ یعنی داشبورد والدین
«شاخص کلی ۶۵» را **قبل از اینکه کودک حتی یک سؤال جواب دهد** نشان می‌داد.
حالا از صفر شروع می‌شود و بر پایهٔ دقت واقعی کودک بالا می‌رود.

## ۱۵) دکمهٔ بزرگ انیمیشنی صفحهٔ اصلی
یک دکمهٔ **۳۹۴×۱۹۵ پیکسلی** با انیمیشن نبض، حلقهٔ گسترش‌یابنده و درخشش اضافه شد.
کودک بدون خواندن هیچ متنی می‌تواند آن را بزند و مسیر ترکیبی
**درس → سوال → بازی → نقاشی** از آسان به سخت برایش اجرا می‌شود و بعد از هر مرحله
خودکار به مرحلهٔ بعد می‌رود (بدون برگشت به خانه).

## وضعیت
| سنجه | قبل | بعد |
|---|---|---|
| سؤال نامربوط به درس | ۲۸ از ۳۲ | **۰** |
| تصویر تقلبی | ۴۹٪ | **۶٪** |
| سؤال خودلودهنده | ۹۵ | **۱۵** (همه معتبر) |
| صدای فارسی | بی‌صدا | **۸ فایل واقعی** |
| شاخص اولیهٔ کودک | ۶۵ ساختگی | **۰** |
| `npm test` | — | موفق (۱۴۶۰ راند) |

---

# مرحلهٔ چهارم: الفبا، صدا، هنر و تنوع محتوا

## ۱۶) درس‌های الفبا ناقص بودند (باگ جدی)
هر درس **۴ حرف** در عنوانش دارد ولی فقط **۳ حرف** تدریس می‌شد: یک حرف فقط «نقاشی» می‌شد
(هیچ‌وقت آموزش داده نمی‌شد) و حرف دیگری دوبار تکرار می‌شد. علت: `plan()` طول درس را
روی عدد ثابت **۵** قفل کرده بود و ۴ حرف در ۵ خانه جا نمی‌شد.
حالا هر درس الفبا = برای هر حرف یک راند «صدا» + یک راند «تصویر» + یک راند نوشتن = **۹ راند**
و **هر ۴ حرف کامل** تدریس می‌شوند.

## ۱۷) صداهای تک‌حرفی (۱۰ فایل جدید)
علاوه بر ۸ فایل معرفی درس، برای **هر حرف یک فایل جداگانه** ساخته می‌شود که دقیقاً هنگام
رسیدن به آن حرف پخش می‌شود. دکمهٔ بلندگو هم همان فایل واقعی را دوباره پخش می‌کند
(قبلاً TTS بی‌صدا را صدا می‌زد).
**۱۰ حرف اول کامل شد: ا ب پ ت ث ج چ ح خ د**

## ۱۸) ۲۲ نقاشی SVG حرفه‌ای جدید
انگور، انار، بستنی، پیراهن، جوراب، جعبه، چای، شمع، صندلی، عینک، عروسک، طبل، لامپ،
قایق، قفل، کلاه، کفش، هویج، هواپیما، چتر، مسواک، رنگین‌کمان.
**تصاویر تقلبی: ۷٪ → ۵٪**

## ۱۹) باگ نقاشی + دفتر بزرگ‌تر + گالری
- **باگ «برگشت»**: عکس‌های undo در مقیاس دستگاه ذخیره می‌شد ولی در مقیاس CSS برمی‌گشت،
  یعنی روی گوشی‌های رتینا نقاشی بعد از undo بهم می‌ریخت. اصلاح شد.
- **دفتر بزرگ‌تر**: از ۲۸۰ به **۴۴۶ پیکسل** (۶۲٪ ارتفاع صفحه).
- **ذخیره و گالری**: دکمهٔ «ذخیره در گالری» + گالری بزرگ در صفحهٔ ستاره‌ها با نمایش تمام‌صفحه.

## ۲۰) پیانوی حرفه‌ای
بازی جدید «پیانوی بزرگ من»: **۸ کلید سفید + ۵ کلید سیاه** با چیدمان واقعی،
حالت **نواختن آزاد** و **۳ آهنگ کودکانه** (ستاره کوچولو، زنگوله‌ها، قایق من) که
کلید بعدی را روشن می‌کند تا کودک آهنگ را یاد بگیرد.

## ۲۱) تنوع محتوا و گروه‌های سنی
- احساسات ۵ → **۱۳**، عادت‌های خوب ۶ → **۱۷**، اعضای خانواده ۶ → **۹**،
  رفتارهای نادرست ۴ → **۱۰** (محتوای تربیتی).
- متن سؤال‌ها متنوع شد (۶ نوع بیان برای سؤال رفتار، ۳ نوع برای احساسات).
- **کودک تیزهوش**: اگر `Adaptive` تشخیص دهد کودک مسلط است (سطح ≥۴) به‌طور خودکار
  به رده‌ی سنی بالاتر ارتقا می‌یابد و محتوای سخت‌تر می‌گیرد؛ و برعکس.
- **عبارت‌های یکتا: ۲۶۰ → ۳۳۷** و کل راندها ۱۴۶۰ → **۱۴۹۲**.

## درصد تکمیل صداها
| بخش | وضعیت |
|---|---|
| معرفی ۸ درس الفبا | ۸ از ۸ — **۱۰۰٪** |
| صدای تک‌تک حروف | ۱۰ از ۳۲ — **۳۱٪** |
| **مجموع فایل‌های صوتی** | **۱۸ فایل** |
باقی‌ماندهٔ ۲۲ حرف در جلسات بعد تکمیل می‌شود (سقف تولید صدا در هر نوبت).

---

# مرحلهٔ پنجم

## ۲۲) صداهای تکراری — حق با شما بود
فایل‌ها تکراری ساخته نشده بودند (هر ۲۰ فایل از نظر بایت یکتا هستند)، ولی **پخش** تکراری بود:
راند «صدا»، راند «تصویر» و راند «نوشتن» هر سه **همان فایل معرفی حرف** را خودکار پخش می‌کردند،
پس کودک یک متن را ۲ تا ۳ بار پشت‌سرهم می‌شنید. حالا فقط راندی که حرف را **معرفی** می‌کند
خودکار پخش می‌شود و بقیه فایل را روی دکمهٔ بلندگو نگه می‌دارند.
**تکرار پشت‌سرهم: ۴ → ۰ (در پخش خودکار)**

## ۲۳) ۱۰ صدای جدید (مجموع ۲۰ از ۳۲)
ذ ر ز ژ س ش ص ض ط ظ اضافه شد.

## ۲۴) ۱۲ نقاشی SVG جدید
مادر، نان، پنجره، دوچرخه، صابون، گیلاس، نهنگ، ببر، تاب، کوه، مدرسه، وزنه.

## ۲۵) ایراد بزرگ رده‌های سنی
- **۱۵۰ درس از ۲۹۲** هیچ «پکیج سنی» نداشتند، یعنی **۷۵۰ راند (نیمی از اپ)** سن کودک را
  کاملاً نادیده می‌گرفتند: کودک ۴ ساله و ۸ ساله دقیقاً یک محتوا می‌دیدند.
  حالا همهٔ راندها تطبیق سنی می‌گیرند. **۱۴۹۲ از ۱۴۹۲ = ۱۰۰٪**
- **سن ۴ و ۵ یکی بودند**. حالا ردهٔ جدید «۴ سال» با ۲ گزینه، عدد تا ۳ و صبر بیشتر.

## ۲۶) تغییر سن در بخش والدین
کارت جدید «پروفایل کودک» در **بالای** داشبورد والدین با ۵ دکمهٔ سنی که با **یک لمس**
سن را تغییر می‌دهد (قبلاً ته صفحه زیر «مدیریت داده‌ها» پنهان بود).

---

# مرحلهٔ ششم

## ۲۷) ۱۰ صدای جدید — مجموع ۳۰ از ۳۲ (۹۴٪)
ع غ ف ق ک گ ل م ن و اضافه شد. فقط **ه** و **ی** باقی مانده (سقف تولید در هر نوبت).
هر ۳۰ فایل با MD5 بررسی شد: **هیچ فایل تکراری وجود ندارد**.

## ۲۸) باگ ۴۰۴ صدا (کشف‌شده در لایو ویو)
هر درسی که فایل صوتی نداشت (ریاضی، منطق و...) درخواست یک فایل ناموجود می‌فرستاد و
در کنسول خطای **۴۰۴** می‌داد. حالا فهرست فایل‌های موجود بررسی می‌شود.
**۴۰۴ها: ۲ → ۰**

## ۲۹) ۱۴ نقاشی SVG جدید
زرافه، فندق، شانه، راکت، ذرت، شکلات، شتر، قوری، برف، ظرف، طناب، رادیو، میز، رودخانه.
**تصاویر تقلبی: ۶٪ → ۳٪** (باقی‌مانده واژه‌های انتزاعی مثل «راز، شاد، جان» است که
اصلاً نباید تصویر داشته باشند).

## ۳۰) سیستم هوشمندتر
- **موتور تطبیقی**: قبلاً **یک** جواب غلط بلافاصله سطح را پایین می‌آورد و سطح مدام
  بالا-پایین می‌شد. حالا دقت **۸ پاسخ اخیر** سنجیده می‌شود: با دقت ≥۸۵٪ ارتقا،
  و فقط با ۲ خطای پشت‌سرهم **و** دقت زیر ۵۰٪ تنزل.
- **مسیر ترکیبی هوشمند**: دکمهٔ بزرگ حالا حوزه‌هایی را که کودک ضعیف‌تر است
  **اول** می‌آورد (بر اساس دقت واقعی)، نه یک ترتیب ثابت.

---

# مرحلهٔ هفتم — تکمیل صداها

## ۳۱) الفبا ۱۰۰٪ کامل شد
دو حرف آخر (**ه** و **ی**) ساخته شد. **۳۲ از ۳۲ حرف = ۱۰۰٪**
همهٔ ۴۸ فایل با MD5 بررسی شدند: **صفر فایل تکراری**.

## ۳۲) صدا برای ریاضی، علوم و هوش هیجانی
۸ فایل آموزشی جدید ساخته شد:
شمارش، جمع، تفریق، اشکال هندسی، حیوانات و صداهایشان، چهار فصل، پنج حس، و احساسات.
این فایل‌ها در ابتدای درس‌های مربوطه **خودکار پخش می‌شوند**.

**پوشش صوتی درس‌ها: ۱۲٪ → ۵۵٪** (۱۶۲ از ۲۹۲ درس)
- ریاضی: ۶۸ درس
- علوم: ۴۳ درس
- هوش هیجانی: ۳۹ درس
- خواندن: ۱۲ درس

## وضعیت نهایی
| سنجه | نتیجه |
|---|---|
| فایل صوتی | ۴۸ (۳۲ حرف + ۸ درس الفبا + ۸ موضوعی) |
| فایل تکراری | ۰ |
| خطای ۴۰۴ | ۰ |
| راندهای رندرشده | ۱۴۹۲ بدون شکست |
| خطای کنسول | ۰ |
| `npm test` | موفق |

---

# مرحلهٔ هشتم — پوشش صوتی ۱۰۰٪

## ۳۳) همهٔ ۲۹۲ درس صدا دارند
۱۰ فایل آموزشی دیگر ساخته شد: هنر، خواندن و درک مطلب، منطق و استدلال، کلمه‌سازی،
حافظه، ترتیب، جمله‌سازی، دسته‌بندی، خلاقیت، و موسیقی.
سپس **همهٔ ۴۵ نوع درس** به فایل‌های صوتی نگاشت شد.

**پوشش صوتی: ۵۵٪ → ۱۰۰٪ (۲۹۲ از ۲۹۲ درس)**
- خواندن ۶۵ · ریاضی ۶۸ · منطق ۴۲ · علوم ۴۳ · هوش هیجانی ۳۹ · هنر ۳۵

## ۳۴) ۲۰ نقاشی SVG جدید
تخم‌مرغ، پنیر، سوپ، چوب، آب، حباب، عکس، تاج، جنگل، فرفره، در، یاس، یوزپلنگ،
بلبل، ویولن، زنگ، زمین، لیمو، عصا، پرنده.

**تصاویر تقلبی: ۳٪ → ۲٪**
۲٪ باقی‌مانده واژه‌های **انتزاعی** هستند (راز، شاد، نظم، جان، ظهر، مست) که
عمداً تصویر ندارند — کشیدن تصویر برایشان گمراه‌کننده می‌شد.

## آمار نهایی
| سنجه | نتیجه |
|---|---|
| فایل صوتی | **۵۸** (۳۲ حرف + ۸ درس + ۱۸ موضوعی) |
| فایل تکراری | **۰** |
| پوشش صوتی درس‌ها | **۱۰۰٪** |
| نقاشی SVG اضافه‌شده | **۶۸** در مجموع |
| راندها | ۱۴۹۳ بدون شکست |
| خطای کنسول / ۴۰۴ | **۰ / ۰** |

---

## مرحلهٔ نهم — موسیقی چندآهنگی، بخش والدین و صفرشدن کامل ایرادها

### ۱) پایان یکنواختی موسیقی
پیش از این تنها **یک الگوی ۳۲ نتی** با سرعت ثابت ۳۸۰ میلی‌ثانیه و یک نوع موج به‌صورت بی‌پایان تکرار می‌شد؛ ریشهٔ اصلی یکنواختی همین بود.
اکنون کتابخانه‌ای از **۶ آهنگ واقعی کودکانه** جایگزین شده است که هرکدام ملودی، تمپو و رنگ صدای مخصوص خود را دارند:

| آهنگ | تمپو | رنگ صدا |
|---|---|---|
| ستارهٔ کوچولو | ۵۰۰ms | triangle |
| رنگین‌کمان شاد | ۳۸۰ms | sine |
| باغ گل‌ها | ۴۴۰ms | triangle |
| رژهٔ شادی | ۳۲۰ms | square |
| لالایی آرام | ۶۲۰ms | sine |
| وقت بازی | ۳۵۰ms | triangle |

- **تعویض خودکار:** با پایان هر آهنگ، پس از ۹۰۰ میلی‌ثانیه مکث، آهنگ بعدی آغاز می‌شود.
- **تعویض دستی:** با لمس دکمهٔ موسیقی، پنجرهٔ «آهنگ‌های شاد» باز می‌شود و کودک یا والدین می‌توانند هر آهنگ را مستقیم انتخاب کنند. آهنگ فعال با رنگ مشخص است و روشن/خاموش کردن موسیقی هم در همان پنجره انجام می‌شود. انتخاب یک آهنگ در حالت خاموش، موسیقی را خودکار روشن می‌کند.

### ۲) بخش والدین
کارت تازهٔ **«پیشرفت در برنامهٔ درسی»** افزوده شد: درصد کل مسیر به‌همراه نوار پیشرفت جداگانه برای هر شش حوزه (خواندن، ریاضی، منطق، علوم، مهارت اجتماعی-عاطفی، هنر) با شمارش دقیق «درس انجام‌شده از کل».
بخش والدین اکنون **۷ کارت** دارد: شاخص رشد، روال تمرین، نمودار مهارت‌ها، تحلیل ۶ بُعدی، پیشرفت برنامهٔ درسی، پروفایل کودک (تغییر سن) و مدیریت داده‌ها.
بررسی خودکار: **۰ سرریز افقی، ۰ دکمهٔ کوچک‌تر از استاندارد لمسی، ۰ خطای جاوااسکریپت**.

### ۳) رفع آخرین ایراد باقی‌مانده
دکمهٔ «راهنما» در بازی حافظه ارتفاع ۳۵ پیکسل داشت و از حداقل استاندارد لمسی (۴۴ پیکسل) کمتر بود. اصلاح شد.
در نتیجه ممیزی عمیق روی **۱۴۹۳ راند** برای نخستین بار به **۰ مشکل** رسید.

### ۴) وضعیت سلامت
- `npm test`: موفق
- ممیزی عمیق: ۱۴۹۳ راند، **۰ مشکل**، ۰ خطای جاوااسکریپت
- پوشش صوتی درس‌ها: **۲۹۲ از ۲۹۲ = ۱۰۰٪**
- ۵۸ فایل صوتی، **۰ صدای تکراری**
- حافظهٔ نهان: `parvaresh-hoosh-v13-music-parent`

---

## مرحلهٔ دهم — ممیزی ظاهری همه‌گانه (سه اندازهٔ نمایشگر × شش صفحه)

ابزار ممیزی خودکاری نوشته شد که **همهٔ** صفحه‌ها را روی سه اندازهٔ واقعی بررسی می‌کند:
۳۲۰ پیکسل (گوشی کوچک)، ۴۱۲ پیکسل (گوشی رایج) و ۷۶۸ پیکسل (تبلت)؛
روی صفحه‌های خانه، انتخاب آهنگ، حوزه، مرحله، جوایز و والدین.

> **نکتهٔ روش‌شناسی:** نسخهٔ نخست ابزار ۱۰٬۸۳۶ «سرریز افقی» گزارش کرد که پس از بررسی معلوم شد **همه‌اش نادرست** بود — درخشش دکمهٔ بزرگ که عمداً با `overflow:hidden` بریده می‌شود و گره‌های نقشهٔ ماجراجویی که عمداً افقی اسکرول می‌شوند. با معیار درست (`documentElement.scrollWidth`) سرریز واقعی **صفر** بود. ابزار بازنویسی شد تا فقط ایرادهای واقعی را بشمارد.

### ایرادهای واقعی که پیدا و رفع شد

| دسته | پیش از اصلاح | پس از اصلاح |
|---|---|---|
| هدف لمسی کوچک‌تر از ۴۴ پیکسل | ۳۷ مورد | **۰** |
| کنتراست پایین‌تر از استاندارد WCAG AA | ۳۴۲ مورد (۲۴ ترکیب رنگ) | **۰** |
| سرریز افقی واقعی صفحه | ۰ | ۰ |
| متن بریده‌شده | ۰ | ۰ |
| خطای جاوااسکریپت | ۰ | ۰ |

**نمونهٔ اصلاحات:**
- شماره‌های مرحله: متن سفید روی مرجانی روشن نسبتِ ۲٫۷۸ داشت. حالا رنگ حوزه با ۲۸٪ سیاه ترکیب می‌شود (`color-mix`) تا عدد سفید همیشه خوانا بماند — رنگ هویتی هر حوزه هم حفظ می‌شود.
- دکمه‌های هدر روی گوشی ۳۲۰ پیکسلی از ۴۰ به ۴۴ پیکسل رسید.
- پیوندهای «حریم خصوصی» و «شرایط استفاده» فقط ۱۷ پیکسل ارتفاع داشتند؛ حالا ناحیهٔ لمسی ۴۴ پیکسلی و زیرخط دارند.
- حدود ۲۰ رنگ خاکستریِ کم‌رنگ در سراسر برنامه تیره‌تر شد تا متن‌های ریز واقعاً خوانده شوند.
- دکمهٔ «پاک کردن داده‌ها» و نشان قفل جوایز به رنگ‌های خوانا تغییر کرد.

### وضعیت سلامت
- `npm test`: موفق · ممیزی عمیق: ۱۴۹۳ راند، **۰ مشکل**
- ممیزی ظاهری: **۰ ایراد** در ۱۸ ترکیب صفحه/دستگاه
- حافظهٔ نهان: `parvaresh-hoosh-v14-contrast-a11y`

---

## مرحلهٔ یازدهم — محتوا، تنوع، هنر حرفه‌ای، فونت و مجوزها

### ۱) ریشهٔ واقعی «کمبود محتوا»
با ابزار اندازه‌گیری مشخص شد مشکل تعداد درس‌ها نبود، بلکه **تکرار** بود:
از ۱۴۹۲ راند تولیدشده تنها **۳۳۵ راند یکتا** بود (۷۷٪ تکراری) و **۴۴٪ درس‌ها** داخل خودشان سؤال تکراری داشتند.

| سنجه | پیش | پس |
|---|---|---|
| راندهای یکتا | ۳۳۵ | **۱۰۰۵** (۳ برابر) |
| درس‌های دارای تکرار داخلی | ۴۴٪ | **۱۱٪** |
| سهم بازی «چهارگزینه‌ای» | ۶۰٪ | **۵۸٪ → ۴۲٪ در سنین بالا** |

**چه شد:**
- بانک‌های محتوا بزرگ شدند: کلمات پرکاربرد ۵۳→۱۱۵، متضادها ۱۵→۳۰، معنی کلمات ۱۴→۲۹، جمله‌سازی ۱۰→۲۵، احساسات ۱۲→۲۶، عادت‌های خوب ۱۶→۲۸، خانواده ۹→۱۶، اعضای بدن ۸→۱۴.
- بانک‌های تازه: **ماه‌های سال**، **جنس اشیا**، **شناور/غرق**.
- **۱۳ نوع راند تازه** که عمداً غیرچهارگزینه‌ای‌اند: وصل‌کردن ماه به فصل، جنس اشیا، شناور یا غرق، وصل حیوان به زیستگاه، وصل عضو بدن به کارش، وصل کلمه به متضاد، وصل احساس به موقعیت، مرتب‌کردن مراحل عذرخواهی/دوست‌شدن/کنترل خشم، جداکردن رفتار مهربان از نامهربان، جمله‌سازی با چیدن کلمات، ترکیب رنگ‌ها و چیدن الگوی رنگی.
- تابع `plan()` بازنویسی شد: هر راند امضای یکتا می‌گیرد و اگر تکراری باشد تا ۸ بار دوباره تولید می‌شود.

### ۲) گروه سنی، حالا واقعاً متفاوت
پیش از این هر سه گروه سنی **دقیقاً یک بازی** را می‌دیدند و فقط تعداد گزینه‌ها فرق می‌کرد. حالا:
- **۴ سال:** بدون ماتریس ریون، بدون ترازو، بدون کشیدن‌و‌رهاکردن، بدون مرتب‌سازی چندمرحله‌ای. بیشتر نقاشی، حافظه، ترکاندن بادکنک و خط‌کشیدن.
- **۵–۶ سال:** حالت میانه با همهٔ ۱۲ نوع بازی.
- **۷–۸ سال (و کودک تیزهوش):** نقاشی و خط‌کشیدن ساده حذف و جایش استدلال، ترازو و وصل‌کردن می‌آید — ۹ نوع بازی با وزن سنگین‌تر روی منطق.

### ۳) هنر حرفه‌ای
- **۳۶ تصویر** که تخت و ساده بودند از نو و با جزئیات کشیده شدند (دست با ناخن و چین کف دست، جعبهٔ سه‌بعدی با چسب، پنجره با بازتاب نور، کرهٔ زمین با قاره، جنگل چندلایه، سوپ با بخار و…).
- همهٔ **۱۲۵ تصویر** حالا **سایهٔ نرم** دارند و از حالت «کلیپ‌آرت تخت» درآمدند.
- یک ایراد در همین کار پیدا و رفع شد: لایهٔ درخشش شفاف، خودش سایه می‌انداخت و بالای هر آیکون لکهٔ خاکستری می‌ساخت.

### ۴) فونت آفلاین
فونت **وزیرمتن** در ۵ وزن از قبل داخل برنامه بود و از اینترنت چیزی نمی‌گرفت، اما **در حافظهٔ نهان سرویس‌ورکر ثبت نشده بود**؛ یعنی در اجرای آفلاینِ سرد به Tahoma برمی‌گشت. اضافه شد.

### ۵) مجوزهای APK
با ابزار رسمی بررسی شد. تنها مجوز: **INTERNET** (لازمهٔ WebView). هیچ مجوز دوربین، میکروفن، موقعیت مکانی، مخاطبین یا حافظه‌ای وجود ندارد. هیچ `uses-feature` سخت‌افزاری هم اعلام نشده.

### ۶) سلامت
`npm test` موفق · ممیزی عمیق ۱۴۹۳ راند **۰ مشکل** · ممیزی ظاهری ۱۸ ترکیب صفحه/دستگاه **۰ ایراد** · ۲۶۷ راند تازه بدون خطا رندر شد · ۰ خطای جاوااسکریپت · ۰ درخواست ۴۰۴.

---

## مرحلهٔ دوازدهم — ماندگاری بلندمدت و نشان‌های واقعی

### ۱) نشان‌ها دیگر تقلبی نیستند
هر شش نشان قبلی فقط به **تعداد ستاره** نگاه می‌کردند. یعنی نشان «استاد الفبا و کلمات» با ۱۵ ستاره باز می‌شد حتی اگر کودک **حتی یک درس خواندن هم باز نکرده بود**. این دقیقاً همان دسته‌بندی تقلبی است که قرار بود حذف شود.

اکنون **۱۶ نشان** داریم که هرکدام به کار واقعی کودک گره خورده‌اند:
- شش نشان مخصوص شش حوزه (۵ درس خواندن، ۵ درس ریاضی، ۵ درس منطق، ۵ درس علوم، ۵ درس مهارت اجتماعی، ۵ درس هنر)
- نشان‌های پیشرفته: قهرمان خواندن (۱۵ درس)، استاد اعداد (۱۵ درس)
- نشان‌های پشتکار: ۳ روز پیوسته، هفتهٔ کامل (۷ روز)
- کاشف شش حوزه: از هر شش حوزه حداقل یک درس
- ماجراجوی بزرگ: ۵۰ درس
- چهار نشان ستاره‌ای (۱، ۱۰، ۵۰، ۱۰۰)

نشان‌های قفل حالا به‌جای کلمهٔ خشک «قفل»، **شرط باز شدن** را می‌گویند: «۵ درس ریاضی را تمام کن». کودک می‌داند باید چه کند.

### ۲) چالش امروز — دلیلی برای برگشتن فردا
ضعیف‌ترین بخش برنامه «ماندگاری» بود: صفحهٔ خانه هر روز **دقیقاً یکسان** بود.
کارت تازهٔ «چالش امروز» بر اساس تاریخ می‌چرخد و هفت حالت دارد: روز کلمه‌ها، روز اعداد، روز کارآگاه، روز دانشمند، روز مهربانی، روز هنرمند و روز رنگین‌کمان (از سه حوزهٔ مختلف درس بخوان).

- چرخش **قطعی بر اساس تقویم** است؛ در طول روز ثابت می‌ماند و فردا عوض می‌شود (آزمون: هشت روز پیاپی، هر هفت حالت).
- نوار پیشرفت زنده دارد و با تمام‌شدن، کارت سبز می‌شود و می‌گوید «آفرین! چالش امروز را بردی.»
- بالای صفحه قرار گرفت تا بدون اسکرول دیده شود.

### ۳) سلامت
`npm test` موفق · ممیزی عمیق ۱۴۹۳ راند **۰ مشکل** · ممیزی ظاهری ۱۸ ترکیب صفحه/دستگاه **۰ ایراد** · ۰ خطای جاوااسکریپت · حافظهٔ نهان `parvaresh-hoosh-v16-retention`

---

## مرحلهٔ سیزدهم — پاکسازی سختگیرانهٔ محتوا (گزارش صادقانه)

هر ایراد گزارش‌شده اندازه‌گیری شد، نه حدس.

### ۱) ستارهٔ بزرگ بی‌ربط — **تأیید شد و رفع شد**
در انتهای تابع انتخاب تصویرِ وسط صفحه، یک `SvgArt.object('star')` به‌عنوان آخرین گزینه بود.
اندازه‌گیری: **۲۲۳ راند (۲۵٪ همهٔ سؤال‌های چهارگزینه‌ای)** یک ستارهٔ طلایی بزرگ و بی‌ربط نشان می‌دادند.
اکنون هر سؤال تصویر مرتبط می‌گیرد: هجاها برای کلمه‌سازی، شکل نام‌برده‌شده برای سؤال شکل، و برای معماها شخصیت متفکر + علامت سؤال. **۲۲۳ → ۵**.

### ۲) سؤال‌هایی که خودشان را جواب می‌دادند — **تأیید شد و رفع شد**
«کدام تصویر با حرف ا شروع می‌شود؟» تصویرِ **پاسخ درست** را وسط صفحه نشان می‌داد. **۱۱۴ راند** این‌طور بودند.
حالا تصویر وسط، **حرف مورد پرسش** است، نه جواب.

### ۳) تصویر اشتباه برای «اسب» — **تأیید شد و رفع شد**
کلمهٔ «اسب» به `horse` نگاشت شده بود ولی چنین تصویری کشیده نشده بود؛ برنامه به‌جایش **صورت روباه (شخصیت برنامه)** را نشان می‌داد. یک اسب واقعی کشیده شد.

### ۴) دفتر نقاشی — **تأیید شد و رفع شد**
بوم با اندازهٔ ۷۷۶×۱۱۳۴ ساخته می‌شد ولی CSS آن را به ۳۷۸×۴۴۶ کوچک می‌کرد. محاسبهٔ محل انگشت این تفاوت را در نظر نمی‌گرفت، پس **قلم جایی دور از انگشت کودک می‌کشید** و گوشه‌های راست و پایین اصلاً قابل دسترسی نبودند. تبدیل مختصات اضافه شد.

### ۵) «۱۰ دقیقه گذشت و به بازی نرسیدم» — **تأیید شد و رفع شد**
یک کودک تازه‌وارد **۶۰+ سؤال چهارگزینه‌ای پشت‌سرهم** می‌دید و **هرگز** به نقاشی یا بازی نمی‌رسید.
حالا هر درس الفبا با خط‌کشیدن در وسط و **بازی بادکنک در پایان** تمام می‌شود: اولین بازی در راند ۱۰.

### ۶) صدای بی‌ربط — **تأیید شد و رفع شد**
نگاشت صدا چند درس را به کلیپ موضوع دیگری وصل کرده بود: بازیافت و حفاظت→«حیوانات»، زمان و مقایسه→«شمارش»، اندازه‌گیری و حجم→«اشکال»، علوم→«حواس».
این نگاشت‌های غلط حذف شدند. پوشش صوتی از «۱۰۰٪ ساختگی» به **۹۱٪ واقعی** رسید — ۲۵ درس ترجیحاً ساکت‌اند تا اینکه دربارهٔ موضوع اشتباه حرف بزنند.

### ۷) کوچک بودن گزینه‌ها و تصاویر — **تأیید شد و رفع شد**
تصویر داخل دکمهٔ گزینه به ۴۶ پیکسل محدود بود. **۴۶ → ۸۲ پیکسل**؛ ارتفاع دکمه ۹۶ → ۱۲۶.

### ۸) شلوغی و اسکرول بیش از حد — **تأیید شد و رفع شد**
صفحهٔ خانه **۱۲ بخش** و **۲٫۲۱ صفحه اسکرول** داشت، با سه ویجت هم‌پوشان که همگی می‌گفتند «بعدی را انجام بده».
کارت تکراری «مسیر پیشنهادی» حذف شد، نقشهٔ ماجراجویی به صفحهٔ حوزه منتقل شد و برنامهٔ روزانه به ۲ مورد محدود شد. **۱۲ → ۱۰ بخش، ۲٫۲۱ → ۱٫۷۷ صفحه**.

### ۹) جدا نبودن بخش بازی‌ها — **تأیید شد و رفع شد**
شهربازی به‌صورت کاشی هفتم داخل «دنیای یادگیری» بود، انگار یک درس دیگر است. حالا بخش جداگانهٔ **«بازی و سرگرمی»** با بنر مخصوص خودش دارد.

### آنچه بررسی شد و ایراد نداشت (صادقانه)
- **قفل‌شدن برنامه:** ابزار خودکار من گزارش داد بازی حافظه و بادکنک «هیچ دکمه‌ای ندارند». بررسی نشان داد **ابزار من اشتباه می‌کرد** — این‌ها `div` با `role="button"` و پشتیبانی صفحه‌کلید هستند که کاملاً درست است. برنامه قفل نمی‌شود.
- **مجوزهای APK:** فقط INTERNET.

### سلامت
`npm test` موفق · ممیزی عمیق ۱۵۰۰ راند **۰ مشکل** · ممیزی ظاهری **۰ ایراد** · ۶۰ راند بازی پیوسته **۰ گیر** · حافظهٔ نهان `parvaresh-hoosh-v17-content-fixes`
